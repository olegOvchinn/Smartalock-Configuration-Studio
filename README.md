# Smartalock Configuration Studio

A static HTML/CSS/JS prototype for designing, configuring and documenting Smartalock locker system deployments. No build step — open any page directly in a browser, or serve the folder with any static file server.

## Start here

Open `smartalock-templates-00.html` (**Project Registration**) — this is the site's actual starting page, and it now lives at the project **root**, not under `pages/`. It has no back-arrow at all (nothing comes before it, and rather than disabling the arrow it's removed outright). Its Contacts card includes a full-width textarea right after IT Contact — "Please provide the names and email addresses of individuals who require access to the Floorsight Administering Web Portal (1-2 people)" — saved as `project.registration.portalAccess`, autosaving on the same debounce as every other field on this page.

From there the flow is:

1. `smartalock-templates-00.html` (root) — Project Registration
2. `pages/smartalock-it-configuration-02.html` — IT Configuration & Integrations
3. `pages/pick-a-product.html` — the product picker, reached only after 00 → 02, not before

`pages/pick-a-product.html` (formerly `index.html` — renamed and moved into `pages/` since it's no longer the entry point) lets the client choose which product line to configure next (**Smartalock** or **Floorsense**), each with a description, icon and a live-switching preview panel. It also has its own "← Back" link to `smartalock-it-configuration-02.html`, since it's no longer the first page in the flow. Choosing **Smartalock** here resumes the wizard at `smartalock-access-methods-09.html` (skipping 00/02, since those are already done) and continues through the rest of the **10-step configuration wizard** under `pages/`, named `smartalock-<module>-0X.html`:

| # | File | Module |
|---|------|--------|
| 00 | `smartalock-templates-00.html` (project root) | Project Registration *(site entry point — done before pick-a-product.html)* |
| 01 | `smartalock-reservation-type-01.html` | Reservation Type |
| 02 | `smartalock-it-configuration-02.html` | IT Configuration & Integrations *(done before pick-a-product.html)* |
| 03 | `smartalock-locker-behaviour-03.html` | Locker Door Behaviour |
| 04 | `smartalock-locker-types-04.html` | Locker Types |
| 05 | `smartalock-locker-policy-05.html` | Locker Policy |
| 06 | `smartalock-user-policy-06.html` | User Policy |
| 07 | `smartalock-kiosk-customisation-07.html` | Kiosk Customisation |
| 08 | `smartalock-configuration-summary-08.html` | Configuration Summary |
| 09 | `smartalock-access-methods-09.html` | Initial Locker Access Methods *(pick-a-product.html's Smartalock card lands here)* |

The actual click-through order (module numbers don't match navigation order) is: **00 → 02 → pick-a-product.html → 09 → 03 → 04 → 01 → 05 → 06 → 07 → 08**. Every wizard page's top progress bar still links to all the others regardless of this navigation order — except 00 and 02, which no longer show a progress bar at all (00 is the entry page and 02 immediately precedes the product picker, so neither needed the full-wizard segment list). Choosing **Floorsense** on the product picker instead routes into its own wizard, `pages/floorsense-<module>-0X.html`:

| # | File | Module |
|---|------|--------|
| 00 | `floorsense-setup-path-00.html` | **Core Setup Complete** — a card-free confirmation/transition step (no more Relaxed/Pre-Configured/Strict/Build Your Own choice); confirms the core policy is done and hands off to the summary |
| 01 | `floorsense-reservation-types-01.html` | Desk Allocation Method (Dedicated Workstations / Reserved by User as Needed / Team Leader Allocated / Booked on Behalf) *(Floorsense wizard entry point — pick-a-product.html's Floorsense card lands here)* |
| 02 | `floorsense-checkin-methods-02.html` | Check-in Methods |
| 03 | `floorsense-desk-user-groups-03.html` | Desk Groups (Neighbourhoods) &amp; User Groups |
| 04 | `floorsense-prebooking-advance-04.html` | Allow Booking in Advance (how many days ahead of time users can book a desk; 0 turns advance booking off) |
| 05 | `floorsense-business-hours-05.html` | Business Hours (standard opening/closing time used for most users) |
| 06 | `floorsense-optional-features-06.html` | **Optional Features** — a hub page listing every optional module below with an on/off switch, off by default |
| 07 | `floorsense-default-reservation-times-07.html` | Default Reservation Times (default booking start, end and duration) |
| 08 | `floorsense-checkin-window-08.html` | Check-in Window (how early/late a user can check in) |
| 09 | `floorsense-idle-release-09.html` | Auto-Release on Idle *(optional; off by default; skipped in click-through unless switched on)* |
| 10 | `floorsense-reservation-length-10.html` | Default Reservation Length |
| 11 | `floorsense-nsnp-11.html` | No Swipe No Power — NSNP *(optional; requires NSNP relay units installed at each desk; skipped unless switched on)* |
| 12 | `floorsense-fme-12.html` | Follow Me Ergonomics — FME *(optional; save/apply a custom sit-stand desk height via the FS mobile app or handset; requires motor integration with a Floorsense handset; skipped unless switched on)* |
| 13 | `floorsense-anti-desk-hogging-13.html` | Anti Desk Hogging *(optional, off by default; restricts booking the same desk repeatedly within a set time window; skipped unless switched on)* |

As with the Smartalock wizard, module numbers don't match click-through order here either: the actual path is **01 → 02 → 03 → 04 → 05 → 06 → 00 → `floorsense-out-of-box-policy.html`** (titled **Configuration Summary**, formerly "Out of the Box Policy" — see below). `floorsense-reservation-types-01.html` is the true entry page (its back-arrow returns to `pick-a-product.html`), then continues through Check-in Methods, Desk & User Groups, Prebooking, Business Hours, and the Optional Features hub — every setting that applies to everyone by default — and finally to Setup Path (module 00), which is no longer a decision point (see below) but a confirmation step whose Continue button leads straight to the Configuration Summary. `floorsense-default-reservation-times-07.html` onward (07–13: Default Reservation Times, Check-in Window, Auto-Release on Idle, Reservation Length, NSNP, FME, Anti Desk Hogging) are **not** part of this enforced click-through path, and — unlike every other reorder in this wizard's history — are also **no longer shown in the progress bar at all**. The progress bar was shortened from 14 segments (the old fixed 00–13 numeric list, shown identically on every page) to exactly 7, now in actual click-through order: Desk Allocation Method, Check-in Methods, Desk & User Groups, Prebooking Window, Business Hours, Optional Features, and Core Complete. All 14 numbered pages carry this same 7-segment bar — the 7 pages actually in the flow show their own segment as `current`; the 7 orphaned pages (07–13) show the identical bar with no segment marked `current`, since none of them are in the list. Those 7 pages are still fully built and functional, but per the client's direction they're meant to be reached only from the Configuration Summary when someone wants to edit a specific setting after the fact, not from the main wizard nav. Three of them (Auto-Release on Idle, NSNP, Sit-stand desks/FME) now have a `.row-edit-link` on the summary's Additional Options group linking straight in; each page's own "Save & continue" button now returns to `floorsense-out-of-box-policy.html` on click rather than continuing forward through the old 07→08→…→13 numeric sequence, so editing one setting doesn't accidentally walk someone through the rest. Their own forward/back page-nav arrows are deliberately left pointing along the old sequence (still driven by the shared `NAV_SEQUENCE`/`seqResolve()` skip-logic on the four optional pages among them — 09, 11, 12, 13 — which transparently skips whichever is switched off on the Optional Features hub) as a separate, manual "browse the other orphaned pages" affordance distinct from the primary Save & continue action. Its state lives under `project.floorsense.wizard` in the same `configurationProject` localStorage key described below — `setupPath`, `reservationTypes`, `checkinMethods`, `deskUserGroups`, `prebookingAdvance`, `businessHours`, `defaultReservationTimes`, `checkinWindow`, `idleRelease`, `reservationLength`, `nsnp`, `fme` and `antiDeskHogging` respectively; the Optional Features hub (module 06) introduces no state object of its own — its four switches read and write those same existing `idleRelease.enabled`/`nsnp.mode`/`fme.mode`/`antiDeskHogging.mode` fields directly, only recording its own `moduleStatus` entry for the progress bar.

The Optional Features hub's Auto-Release on Idle row later gained its own inline hours field (`#idleHoursField`, a small number input shown once that row's switch is on), so someone can set the auto-release duration directly from the hub rather than needing to open `floorsense-idle-release-09.html`. It reads/writes the same `idleRelease.idleHours` field that page's own drum uses, but defaults to 0.5 hours (30 minutes) here rather than that page's 3-hour default, since the hub is meant to offer a fast minimal setting. Separately, the Configuration Summary's Additional Options group was found to be missing an Auto-Release on Idle row entirely (it only listed Maximum desks per user, NSNP, Sit-stand desks, Recurring bookings, and Privacy) — a new "Auto-Release on Idle" row was added directly above NSNP, describing it the same way NSNP is described (off by default, configured separately). That group's single Edit link was first fixed from a stale `floorsense-reservation-length-10.html` target to `floorsense-optional-features-06.html`, then — per a follow-up request — removed entirely in favour of three individual `.row-edit-link` pills, one each on the Auto-Release on Idle, NSNP, and Sit-stand desks rows, pointing at their own dedicated pages (`floorsense-idle-release-09.html`, `floorsense-nsnp-11.html`, and `floorsense-fme-12.html` respectively) — the same "different rows live on different pages" reasoning already used for Prebooking & Duration's two row-level edit links. Recurring bookings and Privacy, previously static text, became real on/off toggles (`recurringEnabled`, default off; `dndEnabled`, default on) matching the fields already used on the additional-policy cards.

Setup Path (module 00) no longer offers a Relaxed/Pre-Configured/Strict/Build Your Own choice — that entire branching decision was removed once the page moved to the end of the core click-through chain (see the paragraph above and the file's own NOTE for the full history of both changes). It's now a single card-free confirmation screen: a short message confirming the core policy (desk allocation, check-in methods, desk & user groups, prebooking window, business hours, optional features) is configured and applies to everyone by default, plus a note that the Configuration Summary also lets you save the configuration as a General Policy and layer on neighbourhood-specific policies for individual Desk Groups. Its Continue button is the wizard's one remaining path into `floorsense-out-of-box-policy.html`, reached with no `?preset=` query string (that page already defaults to its "recommended" copy when no preset param is present). The progress-bar tooltip for this module was renamed from "Setup Path" to "Core Complete" across all 15 Floorsense pages to match; the underlying `data-module="setup-path"` key is unchanged. `floorsense-out-of-box-policy.html` itself was renamed from "Out of the Box Policy" to **Configuration Summary** in its `<title>`, hero heading, and progress-bar tooltip (filename, `data-module="out-of-box-policy"`, and its `outOfBoxPolicy` state key are unchanged — display text only).

The Configuration Summary also delivers on that "layer on neighbourhood-specific policies" note: a conditional **Additional Policies** section, hidden unless Desk Groups and/or User Groups are specifically set up to carry their own different booking policy — i.e. the "apply different policy" checkbox is ticked on `floorsense-desk-user-groups-03.html` (`project.floorsense.wizard.deskUserGroups.{deskGroups,userGroups}.policy === true`), not merely because Desk/User Groups are in use at all for grouping or reporting purposes — **and** only once the core policy above has actually been saved; the client reviews and saves the core policy first, and Additional Policies then reveals itself directly below the now-collapsed core card (rather than being visible, unsaved, at the same time). When shown, a "+ Create New Policy" button lets the client add a new named policy that starts as a clone of the *actual, currently-configured* core policy (Check-in Window, Prebooking & Duration, Check-in Behaviour, Access Methods, and Additional Options/NSNP-sit-stand-recurring-DND — read live from each of those settings' real state, not a fixed snapshot) under a custom name like "Finance" or "Help Desk," then tweak the clone's own numbers/toggles independently and Save it, up to 3 additional policies. Each clone is automatically classified as a **Desk Group** or **User Group** policy (desk if Desk Groups are enabled, otherwise user) — there's no manual toggle for this inside the card, just a read-only badge showing which it is. An info box next to the section spells out that User Group settings take precedence over Desk Group settings (e.g. a Desk Group capped at 1 desk per user is overridden by 15 for a user who's also in a User Group allowing 15) — shown only when both Desk Groups and User Groups are actually in use, since that's the only situation where the precedence rule is relevant. Each policy starts collapsed, showing its name (or an italicised "Untitled policy" placeholder) plus a one-line description ("A custom policy override for this Desk Group/User Group"), its Desk/User Group badge, a status pill, and a small X button to remove it — tapping the row (which the pill itself just reflects) expands it to reveal the actual form controls (number inputs, `.policy-switch` on/off toggles ported from the Optional Features hub, a NSNP `<select>`, and checkbox rows for Access Methods) plus its own "Save Policy" button, which collapses the card and marks it saved; editing a field again after that flips it back to unsaved until saved again. The status pill reads "Saved" once actually saved, or "Collapse"/"Expand" depending on whether the card is currently open — since tapping the header bar only ever toggles expand/collapse (saving itself happens via the dedicated button inside the expanded card), not a generic "Save" label that implied tapping the bar would save it. Trying to collapse a card while it's unsaved is itself guarded: a brand-new policy that's never been saved prompts a confirmation and is removed entirely if confirmed (autosave already persists its in-progress edits, so leaving it collapsed-but-unsaved would just be an orphaned draft); a previously-saved policy with unsaved edits instead prompts to discard those edits and reverts every field back to its last-saved values, rather than deleting a policy that already has a real, confirmed version. Collapsing an already-saved, untouched card is unaffected — no prompt, just a normal collapse. A Desk Group policy card only appears while page 03's own "apply different policy...to different desk groups" checkbox is ticked, and a User Group policy card only while its own "apply different override policy for different User Groups" checkbox is ticked — unticking one just hides that type's cards (nothing is deleted), and the 3-policy cap/count always reflects every stored policy, not just the currently visible ones, so toggling a checkbox off and on can't be used to sneak past the limit. The name field is marked required (a "Policy name*" label plus `required`/`aria-required`) and an unnamed policy can't be saved — clicking "Save Policy" while the name field is empty blocks the save, keeps the card open, and flags the name field with an inline error until a name is entered. Saving also requires at least one setting to actually differ from the core policy as it's currently configured — a named policy whose settings are all still identical to the (live) core policy is blocked from saving too, with an inline hint explaining it needs a genuine difference to count as a distinct policy; the hint clears as soon as a setting is changed and re-checks on every Save Policy click, so changing a value and then changing it back before saving still gets caught. A newly created policy opens expanded automatically, while ones loaded from a saved project start collapsed. Policy cards display saved (collapsed) policies before unsaved (still-being-edited) ones, and within each of those two groups Desk Group policies before User Group policies — a stable sort for display only, echoing the precedence notice's own Desk-then-User framing; the underlying storage order, editing, and removal are unaffected, and saving a policy simply moves it up into the saved group on the next render. Once at least one additional policy exists, the core policy column above shrinks to a denser, secondary-looking layout (reverting the moment the last one is removed), since focus has shifted to the named policies. The whole Review section is a single stacked column — core policy first, Additional Policies directly below it once revealed — rather than a side-by-side layout; the core policy column is re-centred as a single block when neither Desk nor User Groups is in use, since Additional Policies can never apply in that case. Everything autosaves on every change like the rest of the wizard.

The core policy itself is now a single collapsible card rather than six separately-boxed groups: a header bar ("Core Policy" plus a short description of what it covers, and a "Not yet saved"/"Saved" status pill) is always visible and can be tapped at any time to expand or collapse the card, and a "Save Core Policy" button at the very bottom of the expanded card marks it saved and collapses it in one action. That description stays visible (just at a smaller size) even in the denser "compact" view used once Additional Policies exist, rather than disappearing right when the card is most likely to be sitting collapsed and saved. State (`project.floorsense.wizard.corePolicyCard = { saved, collapsed }`) persists independently of the page's own "Confirm & finish" action, so the card reopens or stays tucked away exactly as it was left on a later visit. A styling audit against modules 01-04 confirmed the shared tokens (root variables, brand header, progress bar, hero/eyebrow, card border/radius conventions, action bar, page-nav, footer) already matched exactly; a handful of round-number sizes introduced on this page specifically (the Core Policy header's padding/title/description/status-pill sizing, the guides-submitted callout heading, and the disabled-button opacity) were brought back in line with the wizard's established fractional type scale and sizing conventions.

Every policy card — core, and each additional Desk Group / User Group policy — also carries a **Maximum Desks Per User** fill-in field (default 1), the same figure already referenced in the precedence-notice example above ("...allows 1 desk per user but a User Group...allows 15"). It's a plain number input, editable up to 20, autosaving independently of "Confirm & finish": on the core card it lives at `project.floorsense.wizard.outOfBoxPolicy.maxDesksPerUser`, and on each additional policy it's part of that policy's own object in `project.floorsense.wizard.additionalPolicies`. State lives at `project.floorsense.wizard.additionalPolicies` — a top-level array of up to 3 objects (each with its own `groupType: 'desk' | 'user'`), independent of the core policy's own (still-hardcoded, non-dynamic) summary text. This is purely additive to the Configuration Summary; it isn't wired into "Confirm & finish," the end-user PDF guide, or any other page's Edit links yet.

`assets/documents/floorsense-desk-booking-guide.pdf` — the end-user guide linked from the Configuration Summary's "Explain this policy to your end users" download card — was updated to add a "One desk at a time, by default" item to its Booking & Check-in section, describing the new Maximum Desks Per User setting in plain English (without getting into the Desk Group/User Group precedence mechanics, which aren't end-user-relevant). Its buildable HTML/CSS source, which hadn't previously been committed anywhere in the repo (only the compiled PDF was tracked, so this update required reconstructing the source from the rendered PDF's text and a rendered-page screenshot), is now saved at `assets/documents/src/floorsense-desk-booking-guide.html` so future content changes don't require that reconstruction again. It's rendered to PDF with [WeasyPrint](https://weasyprint.org/) (`weasyprint floorsense-desk-booking-guide.html floorsense-desk-booking-guide.pdf`, or `python3 -m weasyprint ...` if the `weasyprint` command isn't on PATH) — using CSS `@page` margin boxes (`@bottom-left`/`@bottom-right` with `counter(page)`/`counter(pages)`) for the repeating footer rather than a fixed-position element, and inline SVG icons with `stroke="currentColor"` plus a CSS `color:` rule on the containing element (not `stroke:` directly), per the same WeasyPrint quirk noted before — icons render blank otherwise. A literal `·` character is used in the footer's `content:` string rather than a `\00B7` CSS escape, since a CSS hex escape shorter than 6 digits consumes one trailing whitespace character as its terminator, silently swallowing the space that was meant to follow it.

The core policy's Check-in Behaviour group no longer has an Edit link — it never had dedicated fields to edit anyway — and instead has its own two on/off toggles, on by default: "Second swipe checks out" and "Auto-cancel when checking in elsewhere" (`swipeOutRequired`/`autoCancelOnElsewhere`, matching the field names already used on the additional-policy cards). Both autosave to `project.floorsense.wizard.outOfBoxPolicy` independently of "Confirm & finish".

"Confirm & finish" on the Configuration Summary was renamed **"Save & Email Me Summary"** and no longer just shows link buttons or auto-redirects: it still saves `outOfBoxPolicy`/`floorsense.configured`/`moduleStatus` exactly as before, then opens the client's own email app (via a `mailto:` link addressed to "Your Email" from Project Registration) pre-filled with a plain-text summary of the core policy, the **End User Guides** and **Admin Portal Guide** links, and a note that the end-user guide needs opening, printing/saving as a PDF, and attaching manually (mailto links can't carry attachments). The plain-text summary is generated by walking the core policy card's own rendered rows, so it always matches whatever's on screen — including any Check-in Behaviour toggle or Maximum Desks Per User value changed before saving — rather than being a separately hand-maintained copy. A confirmation panel names the actual recipient (or explains that "Your Email" was never filled in) and offers a manual "Continue to Project Home" link, same as before.

The Configuration Summary's "Explain this policy to your end users" download card no longer links straight to the static `assets/documents/floorsense-desk-booking-guide.pdf`. That file has no generation pipeline — it's a one-time hand-authored HTML file rendered to PDF via WeasyPrint and committed as a binary asset — so it couldn't reflect what a given client actually configured in the wizard. The card now opens `pages/floorsense-desk-booking-guide-live.html`, a live JS-populated twin of the same design (same hero/rows/CSS, including the original's `@page` print rules) that reads `project.floorsense.wizard.*` from the shared `configurationProject` localStorage key on load and fills in: the check-in window and its worked example (`checkinWindow`), how many days ahead bookings can be made (`prebookingAdvance`), how long a booking lasts (`reservationLength`, including its free-text description when the "fixed period" mode is used), how many desks a user can hold at once (`maxDesksPerUser`, including the meeting-space-tag note), which check-in methods are actually enabled (`checkinMethods`, built into a plain-language sentence rather than a fixed list), and, shown only when actually turned on, a new "Some desks power off until you check in" row for NSNP (`nsnp` — deliberately left out of the original static guide as "not end-user-relevant" back when it was a vague policy toggle, but genuinely relevant now that it's a concrete relay-driven behaviour) and a "Save your ideal sit-stand height" row for FME (`fme`). Recurring bookings and Do Not Disturb rows show or hide based on `outOfBoxPolicy.recurringEnabled`/`dndEnabled`, and the "Not switched on yet" box is rebuilt from whichever of sit-stand/recurring/DND are off, plus a static "Team bookings, booking on behalf of a colleague, and custom notifications" line with no wizard module behind it yet. A small on-screen toolbar (hidden via `@media print`) has a "Print / Save as PDF" button that calls `window.print()` — the visitor's own browser print dialog produces the PDF using this page's print CSS, so there's no client-side PDF-generation library to maintain. Auto-Release on Idle was deliberately left off this page since its effect is already covered by the check-in-window row from an end-user's point of view. The static PDF and its HTML source under `assets/documents/` are left in place but are no longer linked from anywhere in the wizard.

`smartalock-configuration-summary-08.html` (the Smartalock branch's own, read-only Configuration Summary) got the same treatment. This page previously had no completion action at all — just Print and Download JSON — so it gained a new **"Save & Email Me Summary"** primary button (Download JSON was restyled to a ghost button so there's one clear primary action). Clicking it marks `project.smartalock.configured = true`, saves `moduleStatus.summary = true` (which also marks this page's own progress-bar segment as saved — nothing previously did), builds the same kind of plain-text summary (from this page's own `.summary-group`/`.summary-row` content, all plain label/value pairs here — no toggles or number inputs), and opens a `mailto:` link with that summary plus the same End User Guides link and the Smartalock-specific Admin Portal Guide link ("SMARTALOCK FULL ADMINISTRATION GUIDE" on Notion).

This page later gained the same end-user-guide download card the Floorsense summary already had, plus lost the Download JSON button entirely (per an explicit request — Print is now the only `.ghost` button alongside "Save & Email Me Summary"). The new `pages/smartalock-locker-guide-live.html` is the Smartalock branch's first end-user guide of any kind — there was never a static Smartalock PDF the way `assets/documents/floorsense-desk-booking-guide.pdf` existed for Floorsense — built the same way as `floorsense-desk-booking-guide-live.html`: same hero/rows/footer design (recoloured to this app's Smartalock blue instead of Floorsense green), a "Print / Save as PDF" toolbar button, and dynamic content read from `project.smartalock.wizard.*` on load. It covers: how a locker is obtained (`reservationType.reservationTypes` — admin-assigned, self-reserved with a duration note, and/or parcel delivery, combined into one sentence when more than one applies), how the door opens (`lockerBehaviour.lockerBehaviour`/`pushOpenScope` — Pop Open vs. Push to Open, describing a "specific" scope only as "certain lockers" rather than naming the actual bank/numbers), which access methods are available (`accessMethods.selected`, built into a plain-language list the same way the Floorsense guide lists check-in methods), locker policy (`locker.lockerLimit`, `locker.releaseBehaviour`, and an idle-release row shown only when `locker.idleDuration` is actually set), which kiosk actions are available (`kioskCustomisation.{openEnabled,lockerEnabled,qrEnabled,cardEnabled}`, with a "not available at this kiosk" box listing whichever are off), and which locker types the site offers (`lockerType.selected`/`exampleNumbers`). IT Configuration & Integrations and User Policy's named group details were deliberately left out, same reasoning the Floorsense guide already applies to its own admin-only fields — no end-user-visible behaviour either way. The download card on the summary page and the Save & Email mailto body's "isn't attached automatically" note both mirror the Floorsense page's own wording verbatim.

Desk Groups (Neighbourhoods) & User Groups (module 03) — originally inserted between Check-in Methods and Setup Path, and now sitting between Check-in Methods (02) and Prebooking (04) in click-through order since Setup Path moved to the end of the core chain — was, at the time it was added, the newest addition to the main flow (not one of the optional tail-end modules). It covers two independent topics with the same shape: a multi-select checkbox grid (`.group-check-card`, ported from Check-in Methods' `.desk-check-card`) — "not use" (each grid's first option, tagged **Recommended**), "provide a list/floorplan (or spreadsheet, for User Groups)", and "apply different override policy" — where "not use" is mutually exclusive with the other two but those two can combine freely; and a conditional policy textarea that only appears once the "apply different policy" option is ticked. Desk Group Setup defaults to "not use" (no concrete example was supplied for it); User Group Setup defaults to "provide" + "policy" both checked, since the brief supplied a real example override policy ("Only Client Care can book desks in the Client Care area; Only HR can book desks in the HR area"), prefilled into that field. An amber caution notice (ported from Setup Path's `.setup-notice`) originally repeated at the bottom of each column warning that policy overrides complicate setup and maintenance; it was later consolidated into a single notice shown once at the top of the page, and reworded again into plainer, client-facing language once the admin-facing phrasing proved unclear: "You don't need to set up Desk Groups unless you want different booking rules for a specific area. Likewise, extra User Groups are optional — only add them if your policy really needs different rules for different groups of users, as each one adds a bit more setup complexity."

Prebooking (module 03) and Business Hours (module 04) swapped numbers per a later reorder request, so Prebooking now immediately follows Check-in Methods/Setup Path and Business Hours comes right after it. Business Hours is a single-topic page in the same one-card, one-timeline explainer style as Check-in Window: one card with a live diagram driven by a time drum on either side (Opens / Closes), setting the standard business hours used as a default for most users. Default Reservation Times (module 07, formerly 05) and Check-in Window (module 08, formerly 06) were originally one combined page ("Check-in Policy") hosting two numbered sections; they were later split into their own single-topic pages, each built as a single explainer card: an icon + description, a live diagram, and a footer strip with supporting context. Default Reservation Times comes right after Business Hours (and, since the Optional Features hub was inserted, right after that hub too) so its default start time is already saved by the time a user reaches Check-in Window, whose own worked example depends on it. Auto-Release on Idle (module 09, formerly 07) uses the same card pattern but as its own single-topic page too, since it was originally going to be a third numbered section on the combined page before being split out — its card and diagram were later restyled to match Check-in Window's/Default Reservation Times' Apple Clock time-drum look (a single duration drum driving a fixed "Reserved" pin and an enlarged "Released" pin), replacing an earlier static SVG flowchart and inline number field.

NSNP (module 11, formerly 10), appended after Reservation Length, was the wizard's first genuinely optional module: "No Swipe No Power" keeps a desk's outlets powered off until someone checks in with a valid badge swipe, then off again once their reservation ends (subject to the grace period below); permanently assigned desks are the one exception and stay powered on all the time. It requires NSNP relay units installed at each desk to function. Every piece of copy on the page — hero sub, main description, mode-card descriptions, and the "Other" placeholder — was later reviewed and rewritten to consistently reflect this off-by-default/permanently-assigned-exception logic, replacing an earlier framing centered on "occupied without a valid swipe" that didn't mention permanently assigned desks. That hardware requirement was later turned from a line of hero-sub text into an actual gate: a "We confirm the NSNP relay units are installed" checkbox (unticked by default) sits above the mode grid, and the grid stays visually dimmed and disabled — forced to "We will not be using NSNP" — until it's ticked, so the client can't pick a rollout mode without first confirming the hardware is in place. Its label and hint were later reworded again to drop "installed at each desk"/"every desk" phrasing from the client-visible copy — the point of the checkbox is just to confirm the relay units exist at all, not to make a claim about per-desk coverage. Rather than the single-explainer-card pattern, it uses the same 4-option radio-card grid as Setup Path (`.path-check-grid`/`.path-check-card`) to choose a rollout mode — all desks, a client-supplied list of enabled desks, not using NSNP at all (the default), or a free-text "Other" — plus a supplementary "Power grace period" block that only appears once a non-"none" mode is selected. That block was originally an Apple Clock-style time-drum diagram, later simplified to a plain on/off switch (off by default, meaning power cuts exactly when a reservation ends) plus a fill-in minutes field shown once switched on (default 15 minutes) — described in short, plain language as a way to give a desktop a bit of extra powered time to save and shut down properly after a reservation ends, unlike a battery-powered laptop.

Auto-Release on Idle (module 09) was later made the wizard's second optional module, off by default: its explainer card gained an on/off `.switch` control (the same toggle pattern used on `smartalock-kiosk-customisation-07.html`) in the card header, and its `.timeline-card` diagram plus footer are now wrapped in a `.idle-body` element that stays hidden until the switch is turned on — the same show/hide convention (not opacity dimming) used for NSNP's conditional grace-period block. Its progress-bar tooltip now reads "Auto-Release on Idle (Optional)" across all pages, and its state gained a new `enabled` boolean (`project.floorsense.wizard.idleRelease = { enabled, idleHours }`), defaulting to `enabled: false`.

Follow Me Ergonomics (module 12, formerly 11), appended after NSNP, is the wizard's third optional module: it lets end users save and apply a custom sit-stand desk height via the FS mobile app or handset — this requires the desk's sit-stand motor to be integrated with a Floorsense handset, once wired up the height can be set from either the app or directly on the handset. It reuses NSNP's `.path-check-grid` radio-card pattern but simplified to 3 modes instead of 4 — "All sit-stand desks", "We will provide a list of desk types" (assumed supplied separately, same as NSNP's own list option), and "We will not enable mobile app FME" (the default, off) — with no "Other" option and no supplementary numeric setting, since the brief didn't call for either.

Anti Desk Hogging (module 13, formerly 12), appended after FME, is the wizard's fourth optional module and restricts users from repeatedly booking the same desk, to encourage cross-team interaction. It uses the simplest 2-mode version of the `.path-check-grid` pattern — "We will enable this feature" vs. "We will NOT enable this feature" (the default, off) — and, when enabled, reveals two Apple Clock-style value drums ("Time Window", 1–14 days, default 2; "Number of Same Desk Bookings", 1–7 times, default 1) rather than the rail/pin timeline diagram used elsewhere, since "book the same desk N times in a window" has no natural before/after moment to plot on a timeline. A live summary line mirrors the brief's own example phrasing ("This would allow a user to book the same desk twice every 5 days").

Check-in Methods (module 02) dropped its "Floorsense App at Desk" checkbox option; the prebooking option now sits in that same 2nd-position slot instead of its original 5th position, leaving 4 options in order: Building Access Cards (default), Mobile App or Desktop Browser Prebooking (also default), QR Code, and Wallet Pass. No other page needed updating — `floorsense-checkin-window-08.html`'s footer sentence reads the saved selection dynamically off a label map, so it's unaffected by the option's removal.

The Prebooking card was later marked as a second default-on option alongside Building Access Cards, and its heading/copy were simplified from "Floorsense App or Web Portal Prebooking" (with a trailing "or a kiosk with a swipe card reader" clause) to "Mobile App or Desktop Browser Prebooking," describing pre-booking via the Floorsense mobile app or the my.floorsense.com.au desktop browser portal.

Optional Features (module 06), inserted between Business Hours and Default Reservation Times, is a control hub rather than a feature of its own: a single card listing all four optional modules above (Auto-Release on Idle, NSNP, FME, Anti Desk Hogging) as stacked rows, each with a one-line description and the same `.switch` on/off control used on Auto-Release on Idle's own page, all off by default. It reads and writes the exact same state fields each feature's own page already uses (no duplicate state key), so toggling here and toggling from the feature's own page always agree; switching a feature on for the first time seeds it with a sensible non-off default (`'all'` for NSNP/FME's mode, `'enable'` for Anti Desk Hogging's mode, `true` for Auto-Release's `enabled`) without disturbing any other saved settings on that feature. This hub is also what makes the four optional pages skippable: `floorsense-checkin-window-08.html`, `floorsense-reservation-length-10.html`, `floorsense-nsnp-11.html`, `floorsense-fme-12.html`, and `floorsense-anti-desk-hogging-13.html` each carry a duplicated `NAV_SEQUENCE` array plus a `seqResolve(key, direction)` walker that skips any neighboring optional page whose switch is off, so Next/Back always lands on the nearest enabled (or non-optional) page. The progress bar itself is unaffected — it always shows and links to all 14 modules no matter which switches are on. Its own FME row also carries an inline acknowledgment checkbox, shown only while FME is switched on: "We understand we'll need to provide desk motor details for FME setup," saved to `project.floorsense.wizard.fme.motorDetailsAck` (unchecked by default, unaffected by toggling FME off and back on, cleared by Reset). It doesn't gate anything else on the hub — there's nothing further to configure for FME here — it's a plain acknowledgment, not a hard requirement.

`floorsense-desk-user-groups-03.html` was the one page flagged during that pass as having no content icons at all (just tick + title text on each option card) — a follow-up request illustrated its 6 option cards with the same big bare 56x56px icons, reusing the sitewide icon vocabulary where a good match already existed: both "We will not use ... Groups" (Recommended) options share the established circle-with-slash "off" glyph; the User Group "provide a list ... via spreadsheet" option reuses the established bulleted-list glyph, while the Desk Group "provide ... on floorplan" option gets a new floorplan icon (a room layout with a marked desk) since it's specifically about floorplan markup, not just a list; and both "apply different policy" options share a new policy-document icon (a page with a folded corner), distinct from the clipboard-checkmark icon used elsewhere for "the core policy is ready" so the two don't read as the same concept.

A follow-up request added a banner illustration above `floorsense-desk-user-groups-03.html`'s option cards, depicting the HR/Client Care neighbourhood example already used in this module's own copy (two desk neighbourhoods, each routed to the right group). A hand-drawn isometric SVG in the site's own line-icon style was tried first, but the user preferred their original reference image used as-is, so that image was saved to `assets/images/desk-user-groups-illustration.png` and embedded via a new `.groups-illustration` bordered card (matching the page's existing paper/line/radius tokens), placed between `.page-top-notice` and `.groups-columns`. Once that banner existed, the per-option `.group-icon-img` icons added on each of the 6 option cards were removed again (along with their now-unused CSS rule) — each card is back to its plain tick + title text. The same illustration was then reused on `floorsense-reservation-types-01.html` too, via an equivalent `.types-illustration` wrapper above its option cards — later swapped out for a page-specific illustration the user supplied depicting this page's own three Desk Allocation Method options. That banner was then removed again and the source image split into 3 separate cropped, text-free artwork files (`assets/images/reservation-type-dedicated.png`, `-as-needed.png`, `-ai-agent.png`), one per matching option card, replacing that card's line icon with a wider `.desk-thumb` image instead. The two hidden options (Team Leader Allocated, Booked on Behalf) have no matching artwork and keep their original line icons. The cards were then enlarged and widened (grid/section-head 640px to 760px, card padding and row gap bumped up, `.desk-thumb` 132px to 210px) so the illustrations sit comfortably at a larger size instead of feeling cramped.

The same per-option-illustration treatment was then applied to `floorsense-checkin-methods-02.html` too, using a different user-supplied reference image (one continuous scene with a person per check-in method, linked by workflow arrows through a shared server icon). All 4 cards (Building Access Cards, Mobile App or Desktop Browser Booking, QR Code, Wallet Pass) got their own cropped image and lost their line icon, so `.desk-icon-img` is fully unused on this page (removed). Because these crops came out portrait with inconsistent aspect ratios (unlike page 01's uniform landscape crops), `.desk-thumb` here is sized by height (150px) rather than width, and `.desk-row` uses `align-items: center` instead of `flex-start` so the taller images line up with each card's shorter text block. Grid/card sizing otherwise matches page 01's enlarged treatment (760px grid, wider padding/gaps).

`floorsense-prebooking-advance-04.html` (single "Advance booking limit" card, no multiple options) got the same treatment with a third user-supplied image (an isometric desk scene with "Book 30 Days" badges, a calendar overlay, and +1..+30 day arrows), autocropped to its content bounds and used as-is (no splitting needed, since there's only one card) via a new `.policy-thumb` (260px wide, landscape ~1.39:1). `.policy-icon-img` is fully unused on this page now and was removed; `.policy-row` switched to `align-items: center`; grid/card sizing matches the same 760px enlarge treatment as pages 01/02.

`floorsense-business-hours-05.html` (single `.checkin-card`, already 760px) got a fourth user-supplied image (a 9-5 clock surrounded by a desk switching on, a floorplan/server, and a building) as its header illustration, replacing `.checkin-card-icon` with a new `.checkin-card-illustration` (300px wide, landscape ~1.59:1). Header `align-items` switched to `center` and its gap widened to 24px. The footer's remaining icon (`.checkin-footer-icon`) was then removed too per a follow-up request, leaving this page with zero icons.

`floorsense-optional-features-06.html`'s four `.feature-row`s (Auto-Release on Idle, NSNP, FME, Anti Desk Hogging) each got their own crop from a fifth user-supplied image (one combined scene depicting all four features at once). New `.feature-row-thumb` replaces `.feature-row-icon`, now fully unused and removed; the unrelated footer icon is untouched. Since these four crops landed in a fairly consistent landscape range, sizing is by width like pages 01/04/05 rather than by height like page 02's mismatched portrait crops. Per a follow-up, the card and images were enlarged 30% (`.feature-card` 760px to 988px, `.feature-row-thumb` 170px to 221px).

Per a request to name `floorsense-business-hours-05.html`'s bare 84px accent-blue icon treatment "Style 1" and apply it to page 06, `floorsense-optional-features-06.html`'s 4 `.feature-row-thumb` photo crops were replaced with bare symbolic SVG icons again (restoring the clock/power-button/up-arrow/crossed-repeat-arrow glyphs used before the photo pass), now sized at 84px to match Style 1 exactly; the footer icon was bumped from 56px to 84px too, so every icon on the page shares one consistent size/style with page 05's.

A follow-up review clarified Style 1 is more specific than a bare 84px glyph: every page-05 icon is actually a rounded-square canvas (`<rect x="4" y="4" width="16" height="16" rx="2"/>`) containing a smaller inner glyph in plain full-opacity blue, plus one shape filled at 15% opacity as a lighter "highlight" accent. All 5 page-06 icons (4 rows + footer) were redrawn inside that same canvas frame: Idle now reuses page 05's clock-with-business-hours-pie icon verbatim; NSNP, FME, and Anti Desk Hogging each got a light-blue highlight shape (disc/track) behind a smaller scaled-down version of their existing glyph; the footer toggle icon got a highlight fill on the pill's "on" half behind a smaller pill outline and solid knob.

`floorsense-setup-path-00.html` (the "Core Setup Complete" transition page) got a sixth user-supplied image — a single wide scene summarizing everything the page's copy says is done (checkmarked desks, a floorplan, business hours, three optional features "ON," and a "prebooking ready" calendar). Unlike the other single-card pages, this card's header has no description paragraph beside its icon, so rather than replacing `.complete-card-icon` the image was added as a new full-width `.complete-card-banner` above the existing header, sized to 100% of the card's content width.

`pages/pick-a-product.html`'s still-empty Floorsense "Image placeholder" box (a dashed-border `.page-visual-frame`) was filled in with a seventh user-supplied image — a grayscale isometric office wireframe annotated with four colored feature-callout pill labels (Easy Integration, Realtime Occupancy, Smart Desk Booking, In-depth Analytics). Since the labels run edge-to-edge, a `.page-visual-frame.filled` modifier switches that one instance's aspect-ratio to match the autocropped image exactly (1957:1039) instead of the shared 4:3 placeholder ratio, so the `object-fit: cover` image fills the frame with no cropping and no letterboxing; the dashed border/background become a solid border/white fill once populated. The Smartalock column's placeholder is untouched (still awaiting its own image).

The header brand lockup's two placeholder marks were then replaced with the client's real logo files. The Smartalock wordmark image (`assets/images/smartalock-logo.png`) was swapped in place at its existing path, so every page using the shared `.brand-logo` `<img>` (all 25 wizard pages plus the root `smartalock-templates-00.html`) picked up the new mark automatically with no markup changes. Floorsense didn't have a logo image at all — its half of the lockup was a plain text span (`.brand-logo-floorsense`, bold green "Floorsense") — so a new `assets/images/floorsense-logo.png` was added and that span was replaced with an `<img>` across all 16 files that show the Floorsense side of the header (the 14 numbered Floorsense wizard pages, `floorsense-out-of-box-policy.html`, and `pick-a-product.html`); `.brand-logo-floorsense`'s CSS rule switched from text styling to the same `height: 22px; width: auto` sizing used by `.brand-logo` so both marks sit flush at the same height.

A follow-up shrunk the `.brand-logo` Smartalock wordmark image 30% (22px to 15.4px tall, width auto) across all 26 files that carry it, so it reads smaller relative to the Floorsense logo/tagline beside it in the header.

A follow-up removed the Smartalock logo (and its "×" divider) from all 15 Floorsense wizard pages' header brand lockup, per a request that each product's logo only show on its own branch — Smartalock pages already only showed their own logo, so this made Floorsense pages match. The now-unused `.brand-logo` and `.brand-divider` CSS rules were removed from those 15 files. `pages/pick-a-product.html` keeps both logos, since it's the neutral picker between the two products rather than a page inside either branch.

The Smartalock column's "Image placeholder" box on `pages/pick-a-product.html` was then filled in too, with a user-supplied grayscale isometric locker-room diagram (`assets/images/pick-a-product-smartalock.png`) via its own `.smartalock-visual` aspect-ratio modifier (909/587), matching the treatment already used for Floorsense's box. Floorsense's own image was swapped for a different user-supplied scene (a plain desk-booking office, no colored callout labels) at the same filename, with its frame's aspect ratio moved into a new `.floorsense-visual` modifier (760/543) since the new image's proportions differ from the old one — `.page-visual-frame.filled` itself now only carries the shared border/background, not an aspect-ratio. The page's "Pick Smartalock or Floorsense below to start building its configuration." subtitle was removed at the same time, leaving just the "Choose a Product to Configure" heading.

Each product's visual on `pages/pick-a-product.html` was then consolidated into its own choice card rather than sitting in a separate box above it: the standalone `.page-visual-frame`/`.product-col` wrapper was removed and the image moved inside the `.tcard` itself as a full-bleed top banner (`.tcard-visual`, a plain `<img>` at `width:100%; height:auto` so it keeps its native proportions with no cropping needed — the card switched to `padding:0; overflow:hidden` so its corners clip to the card's own border-radius). Within the card body, the old icon+title `<h3>` was removed and replaced with a `.tcard-brand-logo` image of the product's real logo file (`smartalock-logo.png` / `floorsense-logo.png`, 32px tall), so each card now reads image, then brand mark, then description, with no icon or plain-text title anywhere on it.

A follow-up enlarged just the Smartalock card's image — clarified as "zoom within the same card size" rather than widening the card — by wrapping it in a new `.tcard-visual-frame` (fixed at the image's own 909/587 aspect-ratio, `overflow: hidden`) with a `.tcard-visual-zoom` modifier applying `transform: scale(1.2)` to the `<img>` inside, cropping evenly on all sides so the scene reads 20% bigger without changing the card's or column's width. Floorsense's image is untouched, so the two cards stay the same width.

Fixed the Smartalock card leaving a big empty gap below its paragraph (which also read as its image looking oddly off-center within an over-tall card). The root cause was CSS Grid's default `align-items: stretch` — since Floorsense's description is longer, Grid was stretching the shorter Smartalock card to match Floorsense's height, and since `.tcard` is a plain block element, that extra height just landed as dead space after the last child instead of being distributed anywhere. Added `align-items: start` to `.product-columns` so each card now sizes to its own content and the two cards can differ in height again.

A follow-up moved each card's `.tcard-brand-logo` to a new `.tcard-logo-bar` above the image (card order is now: brand logo, then image, then description), and restored equal card heights: `.tcard` became a flex column with `.tcard-body` set to `flex: 1; justify-content: center`, so when Grid's default `align-items: stretch` makes the shorter card match the taller one, the extra room is absorbed by vertically centering that card's paragraph rather than leaving dead space below it.

Changed `.tcard-visual-frame`'s aspect-ratio from 909/587 (the Smartalock image's own native ratio) to 760/543 (the Floorsense image's native ratio), so the Smartalock image area now renders at exactly the same height as Floorsense's plain image at any given card width, instead of being about 10% shorter. The existing `.tcard-visual-zoom` `scale(1.2)` crop still applies on top of that ratio.

Fixed the Smartalock image crop cutting into the standalone cabinet/person group at its left edge while leaving extra bare floor visible on the right. The source image's content already runs edge-to-edge with no dead margins, so the default center `object-position` was cropping real content on the left and empty floor on the right in equal pixel amounts, which reads as unbalanced since the two sides aren't equally "busy." Set `object-position: 10% center` on `.tcard-visual-zoom` — worked out by replicating the exact `object-fit: cover` + `object-position` + `scale(1.2)` crop math in a Python/PIL script and comparing candidate crops side by side — so the visible window shifts left, keeping that group intact with a small clean margin instead of clipping it.

"Team Leader Allocated" and "Booked on Behalf" on `floorsense-reservation-types-01.html` are visible again — the `hidden` attribute added to both `.desk-check-card` divs during an earlier "temporarily hide until asked to bring them back" request was removed. Pure display change; both cards sit in their original 3rd/4th position between "Reserved by User as Needed" and "AI Agent Auto-Prebooked," still using their original line icons since they have no matching illustration crop.

The newly-unhidden "Team Leader Allocated" and "Booked on Behalf" cards on `floorsense-reservation-types-01.html` were then illustrated too, from a combined user-supplied reference image (one scene per option, captioned above its artwork). Split into `assets/images/reservation-type-team-leader.png` and `reservation-type-booked-on-behalf.png`, with the caption text cropped out via a pixel-darkness scan so it doesn't duplicate the card's own `<h3>`. Both cards' `<svg class="desk-icon-img">` were replaced with `<img class="desk-thumb-tall">`. Unlike this page's other three crops (a consistent ~2.2:1 landscape), these two came out portrait-ish (~1.3-1.5:1) with content running edge-to-edge, so a new `.desk-thumb-tall { height: 120px; width: auto; }` sizes them by height instead of forcing them into the existing width-based `.desk-thumb` and distorting or over-cropping them — the same approach already used for `floorsense-checkin-methods-02.html`'s inconsistent portrait crops. `.desk-icon-img` was removed once these were its last two users.

Per a follow-up request, swapped the "Team Leader Allocated"/"Booked on Behalf" crops' grey card background for white to match this page's other three illustrations. Reprocessed both PNGs with a Python/PIL script: any pixel close to neutral grey (low chroma, i.e. R≈G≈B) at a mid-to-high luminance was blended to white, while the teal/mint artwork colors and dark outline strokes were left untouched. Same filenames, so no markup changes were needed.

Replaced `assets/images/reservation-type-team-leader.png` with a new user-supplied version of the same scene — same pointing team-leader pose and 3-desk arrow layout, but the desk cards now show filled-in named/faced profiles instead of blank silhouettes, plus a sit-stand height readout ("72.5 cm" with up/down controls) at each desk. Already had a clean white background, so this was just autocropped to content bounds. The alt text was updated to match ("unnamed"/"pending check-in" no longer applies).

Swapped in a follow-up variant of the "Team Leader Allocated" scene where the third desk is highlighted in bright green instead of the same teal as the first two. Same filename, so no markup/CSS changes were needed.

The Configured Separately group's Teams/Book on Behalf training tick boxes on `floorsense-out-of-box-policy.html` are now tied to `floorsense-reservation-types-01.html`'s own selections: `trainingTeams`/`trainingBookOnBehalf` tick automatically whenever "team-leader"/"booked-on-behalf" is present in `project.floorsense.wizard.reservationTypes.selected`. This is a live OR with the checkbox's own saved value, not a one-time default — the box stays ticked as long as the type stays selected on page 01 (even if manually unchecked), and only reverts to the last manually-saved value once deselected there. Notifications has no page-01 equivalent and keeps its original manual-only behavior. Verified with a jsdom smoke test covering six cases (each type alone, both, neither, a manual-true with nothing selected, and a manual-false with the type selected).

A site-wide pass made every Floorsense page's content icons the same big 56x56px bare size as `floorsense-reservation-types-01.html`'s `.desk-icon-img` (no circular badge, accent-coloured stroke directly on the svg), and reviewed each one for whether it actually matched its card or section's purpose. Sizes fixed: `floorsense-setup-path-00.html`'s `.complete-card-icon` (48px badge), `floorsense-business-hours-05.html`/`floorsense-checkin-window-08.html`/`floorsense-idle-release-09.html`'s `.checkin-card-icon` (48px badge) and `.checkin-footer-icon` (40px wrapper), `floorsense-optional-features-06.html`'s `.feature-row-icon` (44px badge) and `.feature-footer-icon` (40px wrapper), `floorsense-default-reservation-times-07.html`'s `.path-icon-img` (locally redefined to 38px, out of step with the 56px used by that same class name on nsnp-11/fme-12/anti-desk-hogging-13), and `floorsense-out-of-box-policy.html`'s `.download-card-icon` (40px badge) and `.summary-icon` (32px, shrunk further to 23px in `.compact` mode — that shrink was removed, matching the page's existing text-doesn't-shrink convention). The `.summary-icon` change required restructuring each summary-group's heading markup, since a 56px icon no longer fit inline before the heading text — icon and `<h3>` now sit side by side in a new `.summary-group-heading` wrapper.

Several icons were also redesigned because they didn't clearly represent their feature: NSNP's padlock (security, not power) became a power-button glyph; Follow Me Ergonomics' ambiguous umbrella/lamp shape became the same up-arrow-on-a-pole glyph used for sit-stand height elsewhere; Anti Desk Hogging's calendar-with-checkmark (duplicating the generic "booking" glyph) became a circular repeat-arrow with a slash through it, on both the Optional Features hub and its own page's "enable" option; Auto-Release on Idle's plain X/cancel icon became a clock with an arrow breaking away from it; Check-in Window's checkmark-in-circle became a clock with a highlighted wedge depicting a specific time range; the Configuration Summary's generic bar-chart icon for "Additional Options" became a sliders/toggles glyph, matching that it's just a list of on/off features; and one of the two default-reservation-times cards reused the exact same clock-with-ticks glyph as its sibling card, so it was changed to a sunset icon to distinguish "ends at close of business" from "ends after N hours." Small UI chrome (chevrons, switches, checkboxes) and status/notice glyphs sitting in small aside boxes (warning triangles, info icons, the post-submit checkmark) were deliberately left at their existing sizes — forcing those to 56px would look absurd relative to their compact context, and the request was about icons that identify a card or section's purpose, not every small indicator on the page. `floorsense-desk-user-groups-03.html` and `floorsense-reservation-length-10.html` have no content icons at all and were left as-is (not asked to add new iconography where none existed), and `floorsense-desk-booking-guide-live.html` was left untouched since it's a printable end-user guide with its own established rounded-square badge design language, not part of the interactive wizard's card visual system.

The core policy card was widened another 10% on top of that (832px to 915.2px, `.compact` 598px to 657.8px), and `.core-policy-desc`'s copy was reworded from "The default booking policy — check-in window, prebooking limits, access methods, and more — that applies across the board unless a Desk Group or User Group policy overrides it below." (which re-listed section names already shown as headings just below) to "Your organization's default booking policy — it applies to everyone unless a Desk Group or User Group policy overrides it below.", stating the card's purpose rather than its contents.

The Configuration Summary's core policy card (`#coreSettingsColumn`) was widened 30% per request — `max-width: 640px` to `832px`, and its `.compact` variant (once at least one additional policy exists) from `460px` to `598px`, both still centred. Along the way, found that the page's base `.download-card`/`.summary-group` `max-width` rules are dead code on this page: every instance of both lives inside `.core-policy-body`, whose own higher-specificity rules always set `max-width: none` and let those elements fill whatever width `#coreSettingsColumn` (or its `.compact` variant) actually provides.

The Configuration Summary's card typography was compared against `floorsense-setup-path-00.html`'s `.complete-card` (title 19.5px, paragraph 14.76px — the wizard's reference card scale) after a report that all its card paragraphs and headers looked too small. `.summary-desc` (the description text under each summary-group row) was bumped from 12.9px to the established 14.76px paragraph baseline. More significantly, `.core-settings-column.compact` (applied once at least one additional policy exists — not an edge case, since it only needs a Desk/User Group policy override plus one saved policy) was shrinking headings and paragraphs well below their normal size on top of that — core policy title down to 15px, its description to 11px, summary-group headers to 16.5px, and several others — making every card look noticeably smaller than the rest of the wizard. Removed all of compact mode's font-size overrides for headings and paragraph text; it now only tightens max-width, padding, and icon size, so text stays the same size in both states.

Selecting a Desk Group or User Group override on `floorsense-desk-user-groups-03.html` was found to make the Configuration Summary's content jump to the left edge instead of staying centered: `#coreSettingsColumn`'s centering had been tied to a `.centered` modifier class applied only when no group override was in use, so as soon as a group's "apply different policy" checkbox was ticked (but before any additional policy existed yet, so the narrower `.compact` modifier hadn't kicked in either) the column had no width cap or margin of its own and its 640px-capped children rendered flush left. Fixed by making centering (`max-width: 640px; margin: 0 auto;`) the unconditional base rule on `.core-settings-column`, with `.compact` only narrowing it to 460px (still centered) once additional policies exist; the same flush-left bug and fix applied to the Additional Policies section's own content (`.policies-section-head`, `.policies-intro`, `.policies-grid`, and `.add-policy-row`, whose own stale `max-width: 1064px` — another leftover from the old two-column layout — was reduced to 640px to match its siblings).

The Configuration Summary's Additional Policies list (`.policies-grid`, `#policiesList`) turned out to still be a leftover 2-column CSS grid (`grid-template-columns: 1fr 1fr`) from an earlier design generation where the whole page was genuinely two columns (core policy in a narrow left track, Additional Policies in a wider right track). That outer two-column page layout was removed in a later change, but the inner grid was never cleaned up with it — harmless while confined to a ~460px-wide track, but once the page became one full-width stacked column, 2-3 additional policy cards could render as a real side-by-side right column. Changed `.policies-grid` to a single-column flex stack (`max-width: 640px`, matching `.summary-group`/`.download-card` above it) and removed the now-unnecessary 900px collapse breakpoint.

A site-wide audit checked every Floorsense page for single-column content that wasn't horizontally centered the way `floorsense-setup-path-00.html`'s `.complete-card { max-width: 720px; margin: 0 auto; }` is. Seven pages needed a fix: `floorsense-reservation-types-01.html`, `floorsense-checkin-methods-02.html`, and `floorsense-fme-12.html` (each `.section-head` + option grid, `max-width: 640px`, gained `margin: 0 auto`); `floorsense-prebooking-advance-04.html` (`.section-head` and its single-column `.policy-grid.single`); `floorsense-reservation-length-10.html` (`.section-head`, `.duration-options`, `.duration-fields`, `.toggle-card`); `floorsense-nsnp-11.html` (`.section-head`, `.path-check-grid`, `.nsnp-relay-confirm`, `.nsnp-grace-block`); and `floorsense-anti-desk-hogging-13.html` (`.section-head`, `.path-check-grid`, and the 760px-wide `.adh-settings` drum block) — in each case adding `margin: 0 auto` (or `margin: Npx auto 0` where a directional top/bottom margin already existed), without changing any existing max-width. Pages `05`, `06`, `07`, `08`, and `09` needed no change: `07`'s `.section-head`/`.policy-diagram-frame`/`.policy-subgroup` CSS rules turned out to be dead leftovers from an earlier redesign (its real, live wrapper is `.cards-wrap { max-width: 760px; margin: 0 auto; }`, already centered), and the rest already use an equivalent centered single-card pattern (`.checkin-card`/`.feature-card { max-width: 760px; margin: 0 auto; }`). `floorsense-desk-user-groups-03.html` is explicitly two-column and not applicable, and `floorsense-desk-booking-guide-live.html` doesn't share this wizard's `.panel-inner` layout at all.

`pages/dashboard.html` and `pages/reports.html` both link back to `pages/pick-a-product.html` via a **"Configuration Wizard"** nav item, and their **"Summary"** nav item links to `smartalock-configuration-summary-08.html`.

### Conventions used across the 10 wizard pages

- **Shell markup**: `.page-main` > `.panel-topbar` (`.top-frame` with `.brand` + horizontal `.progress-bar`) + `.panel-intro` (hero title/subtext) → `.panel-scroll` (the actual form content) → `.action-bar` (Reset / Save & continue).
- **Progress bar**: `.progress-seg` per module; `.saved` = previously completed, `.current` = active page, hover shows a tooltip with the module name.
- **Typography**: Inter throughout. Hero titles are 76px/weight 200, forced onto a single line via `white-space: nowrap` + a `width:100vw; left:50%; transform:translateX(-50%)` breakout so long titles don't get clipped by their column.
- **State**: each page reads/writes a single `localStorage` key, `configurationProject`, shaped roughly as:
  ```
  {
    id, created, updated,
    registration: { customer, site, projectName, contact, email, phone, ... },
    smartalock: {
      configured, templates,
      wizard: {
        moduleStatus: { registration: true, it: true, ... },  // drives the progress bar
        it: {...}, accessMethods: { selected: [...], cardAssignMethod, pinAssignMethod }, ...
      }
    },
    floorsense: { configured, templates, wizard: {} }
  }
  ```
  Each page's own section of `wizard` is independent — check the `<script>` block at the bottom of any page for its exact fields.
- **Images/diagrams**: pages with a right-hand visual panel (e.g. IT Configuration's architecture diagram) swap the `<img src>` via JS based on form selections — see `ARCH_VISUAL` in `smartalock-it-configuration-02.html` for the pattern.

## The rest of the site

- `smartalock-templates-00.html` (project root) — the site's actual entry point (see "Start here" above).
- `pages/pick-a-product.html` — the product picker (formerly the root `index.html`; renamed and moved into `pages/` since 00 → 02 now happen before it). It no longer links to Dashboard, Configuration Engine, Implementation Ready, or Reports — those pages predate the product-picker flow and are currently only reachable by opening their file directly (`pages/dashboard.html`, `pages/reports.html`, `pages/smartalock-wizard.html`, `pages/smartalock-templates.html`) or via each other's own top nav.
- `pages/dashboard.html`, `pages/reports.html` — surrounding app shell (predates the wizard work above).
- `pages/smartalock-wizard.html` — a separate reference/spec document (not part of the numbered wizard family) that the wizard's field definitions were originally researched from.
- `pages/register.html`, `pages/project-home.html`, `pages/ai-policy-workshop.html`, `pages/smartalock-templates.html` — other standalone app pages, unrelated to the wizard.
- `assets/`, `css/`, `js/` — shared images/icons, stylesheets and scripts for the pages above. Each of the 10 wizard pages is self-contained (styles and script inlined in the page itself) and doesn't depend on `css/`/`js/`.
- `data/`, `docs/`, `output/` — supporting data files, spec docs, and generated report output; not touched by the wizard work.

## Housekeeping notes

- Three earlier generations of this same wizard (`-v3`, `-v4`, `-v6` suffixed pages, plus an older unversioned `configuration-summary.html`) were removed as of this handover — the numbered `smartalock-<module>-0X.html` family above is the only one still live and linked from the main nav.
- The old marketing-style `index.html` (Dashboard / Configuration Engine / Implementation Ready / AI Policy Workshop cards) was replaced by the Smartalock/Floorsense product picker described in "Start here" (`pages/configuration-start.html` was moved to the root as that generation's `index.html`). Since then, the flow was restructured again: `smartalock-templates-00.html` and `smartalock-it-configuration-02.html` were promoted ahead of the product picker, and the picker itself was renamed from root `index.html` to `pages/pick-a-product.html`. There is no `index.html` anywhere in the project any more — the root now holds `smartalock-templates-00.html` instead.
- A few files could not be permanently deleted due to a filesystem restriction in the environment this cleanup was done in; they were moved to a git-ignored `_removed-for-handover/` folder instead (including the old root `index.html`, at `_removed-for-handover/index.html.old`). That folder is safe to delete outright from Finder/Terminal — it isn't referenced by anything and isn't tracked in git.
- If you're picking this up on a machine with GitHub access already configured, remember to `git push origin main` — recent work was committed locally but may not be pushed yet.
- Floorsense's brand accent was changed from green to blue, matching Smartalock. All 15 `pages/floorsense-*.html` wizard pages had their `:root` tokens changed from `--accent: #1FAA59; --accent-wash: #E6F7ED; --accent-deep: #178A48;` to `--accent: #0071E3; --accent-wash: #E8F1FC; --accent-deep: #0058B0;` (the same blue Smartalock already used). `pages/pick-a-product.html`'s `.tcard[data-type="floorsense"]` choice card got the matching `--tc`/`--tc-wash` update, so both product cards on that page now share one accent color. `pages/floorsense-desk-booking-guide-live.html` (which hardcodes colors rather than using CSS variables) got the same one-for-one hex swap. `smartalock-reservation-type-01.html`'s unrelated `.tcard[data-type="user"]` variant, which happened to reuse the same green hex for an unrelated internal card type, was deliberately left untouched. See each touched file's own closing `MOST RECENT CHANGE` note for detail.
- `pages/floorsense-reservation-types-01.html`'s "Dedicated Workstations" card got a new illustration (`assets/images/reservation-type-dedicated.png`, a dual-monitor desk with a verified-user ID badge callout). Since its aspect ratio is roughly square rather than the old ~2.2:1 landscape crop, the card switched from `.desk-thumb` to the `.desk-thumb-tall` class already used by the Team Leader Allocated / Booked on Behalf cards, keeping all differently-shaped illustrations a consistent height.
- Per a request to "review the icons on page 01, make them all more symbolic and can only use blue", all 5 of that page's photographic `.desk-thumb`/`.desk-thumb-tall` illustration crops were replaced with bare symbolic `.desk-icon-img` SVG line icons (56x56px, single `--accent` blue via `currentColor`, no other hue anywhere): a map-pin (Dedicated Workstations), three desk tiles with a checkmark (Reserved by User as Needed), an org-chart glyph (Team Leader Allocated), an ID card with a person and a "+" (Booked on Behalf), and a robot face (AI Agent Auto-Prebooked). The five `reservation-type-*.png` crops are no longer referenced anywhere but were left in place rather than deleted.
- A follow-up removed that page's `.desk-tick` checkbox marker (the small square that filled solid blue with a checkmark once selected) from all 5 cards, per "remove tick boxes but leave cards clickable" — each row is still a `<label>` wrapping the actual checkbox, so clicking anywhere on a card still toggles selection; `.desk-check-card.checked`'s border/background tint is now the only selected-state indicator.
- `pages/floorsense-business-hours-05.html`'s Apple Clock-style time drums were removed and replaced with directly-draggable rail pins, and the rail now shows a tick at every 30-minute mark (hour marks taller/darker). Dragging or arrow-keying a pin snaps to the nearest 30 minutes, mutually clamps against the other pin (minimum 1-hour gap) and against the rail's own 6:00 AM–10:00 PM span. Verified with a jsdom smoke test that drives real pointer/keyboard events, since WeasyPrint doesn't execute JavaScript and can't render this page's JS-positioned diagram at all (confirmed true of the pre-edit version too).
- That same timeline diagram got a purely visual refresh after feedback that it "looks dated": a thicker pill-shaped rail with an inset groove shadow, tick marks moved to a quiet dotted row below the rail instead of crossing through it, larger pins that shift color on hover/drag, and the floating time labels restyled as solid pill chips instead of bare text. No markup, JS, or behavior changed.
- The same drum-to-draggable-pin treatment was applied to `pages/floorsense-idle-release-09.html`: its single time drum was removed and the "Released" pin is now directly draggable (the fixed "Reserved" pin at the rail's left edge stays non-interactive), with 30-minute ticks along the whole 0–24h rail. A new `.draggable` CSS modifier scopes drag affordance (grab cursor, enlarged hit target, hover/focus glow) to only the interactive pin, since — unlike business-hours-05 — not every pin on this page is draggable. Verified the same way, via a jsdom pointer/keyboard smoke test.
- Same treatment again on `pages/floorsense-checkin-window-08.html`: its earliest/latest time drums were removed and the two dots either side of the fixed "Booking start" pin are now directly draggable, each confined to its own half of the rail and clamped to a 30-minute-minimum gap from center, with 30-minute ticks on both halves. This reuses the drum-era offset-from-center scale (3-hour max offset, 10% minimum visual distance from center) rather than reverting to the page's older non-uniform slider scale from earlier in its history. Verified with the same jsdom pointer/keyboard approach.
- Same treatment on `pages/floorsense-default-reservation-times-07.html`, the most complex case (three separate cards, each with its own drum and rail): card 1's start-time pin is now draggable across a full-day rail; cards 2 and 3's "Ends" pins are draggable along their existing fixed-left-anchor rails (card 2 as a duration, card 3 as an absolute end time computed from card 1's live start time), while each card's "Starts" pin stays fixed and read-only. A single generalized `makeDraggablePin()` helper handles all three, each finding its own rail via `.closest('.timeline-track-wrapper')`. Every rail got 30-minute ticks on its own scale. Verified with a jsdom test covering all three cards, including that dragging card 1's pin live-updates cards 2/3's "Starts" labels.
- `pages/floorsense-business-hours-05.html`'s `.checkin-card` header illustration (`assets/images/business-hours-illustration.png`) was replaced with a new hand-authored isometric illustration in the site's blue palette, per a request for "a simple slick, cool illustration for what is business hours start and end using ISO style" — out with the old cluttered green/teal five-element scene (desk, floorplan/server, clock badge, office layout, building) left over from before the blue rebrand. The new artwork is a sunrise puck labeled "9:00 AM / ARRIVES" and a crescent-moon puck labeled "5:00 PM / DEPARTS" connected by a soft gradient arc, each puck built from a two-ellipse-arc path for a smooth isometric coin/cylinder profile. Authored as SVG, rasterized via cairosvg, autocropped via PIL/numpy. This asset is only referenced on this one page. The `<img>`'s alt text was updated to match the new scene.
- That same page's timeline rail got a further visual pass, per a user-supplied reference image of a segmented-capsule time picker. The filled Arrival–Departure zone now renders as a row of individually rounded 30-minute blocks with small gaps between them (a `renderZoneSegments()` helper regenerates them on every render, colored via a new `lerpColor()` interpolation from deep blue to light cyan) instead of one solid gradient pill, and the surrounding rail grew from 6px to 24px tall with a lighter bordered "casing" look. Only the reference's segmented-block concept was adopted — its diamond-grid backdrop and metallic cone pins were deliberately left out to keep the control consistent with the site's flat design language; the existing round drag-handle pins are unchanged.
- Two more tweaks to that same diagram: the "Standard hours: 9:00 AM – 5:00 PM" subtitle line above it was removed as redundant (the floating time chips above each pin already show the same values), and each pin's visible round knob was hidden (`opacity: 0`) while staying fully draggable/keyboard-operable via its existing invisible hit-target — so the diagram now reads as just the segmented bar plus two floating time chips, with no visible subtitle or pin knob.
- The segmented blocks got a bright blue glow (`box-shadow: 0 0 6px 1px rgba(10,132,255,0.85), 0 0 14px 2px rgba(10,132,255,0.5)`, layered on top of each block's own gradient fill and inset highlight) so the filled zone reads as one continuous luminous strip, and the rail grew another 50% taller (24px to 36px) per two more follow-up requests.
- One more follow-up shrank the blocks themselves 30% (their vertical inset within the rail grew from 3px to 7.5px top/bottom, so segment height went from 30px to 21px) while leaving the now-36px rail's own height untouched, per "the height of the rail is good but make the blocks inside 30% less tall".
- The outer bright-blue glow was replaced with an inward "blue glass" look, per "add some glow inside like if they are made of blue glass" (clarified with "no need glow outside"): the segments now use an inset cyan glow, a brighter top inset highlight, a darker bottom inset shadow for depth, and a new glossy top-to-bottom white sheen overlay (`.timeline-zone-seg::before`) layered over each block's own gradient color.
- The header illustration was swapped back out for a basic square icon (`<svg class="checkin-card-icon"><rect .../></svg>`, 56x56px, `stroke="currentColor"` colored via `--accent`), matching the plain-icon convention every other single-card Floorsense page uses. `assets/images/business-hours-illustration.png` is now unreferenced but was left in place rather than deleted.
- That square icon became an actual clock face and grew 50% larger (56px to 84px), per "make it sqiaire clock, 50% larger with business hours highlite": added hour/minute hands (pointing to 12 and 3) plus a filled pie-slice sector spanning 9→12→5 o'clock (the 8-hour span a 9-to-5 workday covers) as a soft `currentColor`-tinted "business hours" highlight behind the hands.
- The departure time chip's near-black `.label-time.primary` background became `var(--accent)` (bright blue) instead of the old navy `#102A43`, per "label-time primary make blue instead of black" — the arrival chip's `var(--accent-deep)` background keeps the two labels visually distinct, just both blue now.
- Per an immediate follow-up ("make it instead same colour as the one to the left"), `.label-time.primary` changed once more to `var(--accent-deep)`, matching the arrival chip's color exactly rather than using two different blues.
- Per a request to rebuild every other Floorsense timeline to match business-hours-05's final look, `pages/floorsense-idle-release-09.html`'s diagram got the same full treatment: 36px pill-casing rail, segmented glass-glow blocks (`renderZoneSegments()`/`lerpColor()` ported verbatim) in place of one solid gradient bar, invisible pin knobs, dotted ticks below the rail instead of crossing it, and the "Auto-release after: 3h" subtitle removed. The page's old `.big-anchor` emphasis (a giant 66px pin/22.5px label reserved for "Released") was retired too, since with pins now uniformly invisible it no longer matched the new design language — "Released" stays distinguishable by being the one draggable pin, not by size.
- Same treatment on `pages/floorsense-checkin-window-08.html`: 36px pill-casing rail, both the pre-window and post-window zones now render as segmented glass-glow blocks (`renderZoneSegmentsInto()`, with the post-window side's gradient reversed so both zones still read light-near-center/deep-at-the-edges), dotted ticks below the rail, every pin (the draggable `.handle` dots and the fixed center `.anchor-pin`) invisible, and the "Booking start time: 8:30 AM" subtitle removed. The page's old `.center-node` emphasis (66px pin, 22.5px label) and its `ci-pop-handle`/`ci-ping` reveal animations were retired for the same reasons as idle-release-09's `.big-anchor`.
- Per "regenerate icons on page 01 so they are squire shapes reate ios style glass like see throu icon, only use blue and white with a bit of blue glow from the inside", `pages/floorsense-reservation-types-01.html`'s 5 symbolic `.desk-icon-img` line icons were each wrapped in a new square 60px `.desk-icon-badge` — a translucent blue gradient with `backdrop-filter: blur()/saturate()` frosted-glass blur, a translucent white border, an inset box-shadow stack (white top highlight, dark bottom depth-shadow, blue inner glow) and a glossy sheen overlay, the same "blue glass" technique already used on business-hours-05's timeline segments. Each glyph recolors to white (`.desk-icon-glyph`) with bumped `fill-opacity` for contrast against the glass. Only blue and white appear anywhere in the result.
- Per a follow-up to flip that badge to "white background but blue lines instead, make lines thinner and sharper, and increase size of the symbols inside the icon canvases by 15%", `.desk-icon-badge`'s gradient/glow recolored to a white glass with a blue-tinted inset glow, `.desk-icon-glyph` switched from white to `var(--accent)` blue and grew from 30px to 34.5px, and each of the 5 SVGs got a thinner stroke (1.6→1.3), sharp `butt`/`miter` caps and joins instead of `round`, and reverted fill-opacity values now that the fill color is light blue against white rather than white against blue.
- Per "keep shapes of the icons but remove glow and glass shadowing, instead make blue canvas border line", `.desk-icon-badge` dropped its backdrop-filter/gradient/inset-shadow glass treatment (and the now-unused `::before` sheen rule) entirely, becoming a plain flat white square with a `1.5px solid var(--accent)` blue border. The glyph shapes, size, and colors themselves were untouched.
- Per "review page 02 icons to match page 01 icon styling", `pages/floorsense-checkin-methods-02.html`'s 4 photographic `.desk-thumb` illustration crops (Building Access Cards, Mobile App or Desktop Browser Booking, QR Code, Wallet Pass) were replaced with bare symbolic SVG icons in page 01's current flat `.desk-icon-badge`/`.desk-icon-glyph` styling (white square, blue border, thin sharp blue lines, no glow). `.desk-thumb` and its portrait-image sizing were removed; `.desk-row` alignment reverted from `center` back to `flex-start` to match page 01.
- Per a request to "simplify refine and adopt" a new user-supplied reference image for `floorsense-desk-user-groups-03.html`'s banner, the raw screenshot (an isometric HR/Client Care office scene with unrelated dashboard chrome bleeding into all 4 corners) was cleaned up via image editing: the 4 extraneous corner widgets were masked out with locally-sampled background fill, the reference's mint/teal accents were hue-shifted to the site's blue (excluding the two people's own green clothing), and the result was cropped to content and saved as `assets/images/desk-user-groups-illustration-v2.png`, replacing the old illustration (left in place, now unreferenced) as the page's `<img>` src.
- Per "remove grey card borders, instead do fuzzy shadow as if each card is transparent glass", `floorsense-reservation-types-01.html`'s `.desk-check-card` dropped its solid grey border entirely, replaced with a translucent white background (`rgba(255,255,255,0.6)`) plus `backdrop-filter: blur()` and a soft, fuzzy `box-shadow` (large blur, no hard edge) for depth instead of an outline. `:hover`/`.checked` states now read through the shadow's tint/spread and the background tint rather than a border-color change.
- Per "apply light colour touch to the icons: first icon blueish next pinkish, purplish itc", the same page's 5 `.desk-icon-badge` cards each got their own light accent colour instead of one shared blue, in card order: blue `#4A8DF0`, pink `#EC6BA5`, purple `#9B6BEC`, teal `#2FBF9F`, amber `#F2A154` (the last two continuing the pastel progression since only 3 colours were named). Each colour sets both the badge border and its glyph's stroke/fill via a modifier class.
- Per a follow-up ("each icon should drop short light shadow"), `.desk-icon-badge` gained a small `box-shadow` (short, low-opacity) for a subtle lift, distinct from the card's own larger fuzzy glass-shadow.
- Per a request to propagate `floorsense-reservation-types-01.html`'s latest card/icon styling sitewide, `pages/floorsense-checkin-methods-02.html`'s `.desk-check-card` swapped its grey border for the same translucent-glass background/fuzzy-shadow treatment (with hover/checked read through the shadow instead of a border color), and its 4 `.desk-icon-badge` icons picked up page 01's per-icon light accent colours (blue/pink/purple/teal) plus its short drop shadow.
- The same glass-card propagation was applied to `pages/floorsense-desk-user-groups-03.html`'s `.groups-card` (the two Desk/User Group column wrappers) and `.group-check-card` (the per-option checkbox cards inside them); this page's option rows carry no icon markup at all, so no icon changes were needed.
- `pages/floorsense-prebooking-advance-04.html`'s single `.policy-card` also picked up the glass-card treatment (no hover/checked state existed to convert); its one `.policy-thumb` illustration is a large 260px photographic crop rather than a small symbolic glyph, so it was deliberately left out of the 60x60px icon-badge treatment used on pages 01/02 to avoid clipping/distorting the artwork.
- `pages/floorsense-business-hours-05.html`'s `.checkin-card` also picked up the glass-card treatment; its `.checkin-card-icon` "Style 1" clock glyph and the unrelated `.timeline-rail` were deliberately left untouched, per an explicit request to preserve Style 1 by name.
- `pages/floorsense-optional-features-06.html`'s `.feature-card` also picked up the glass-card treatment; its `.feature-row-icon`/`.feature-footer-icon` "Style 1" icons were deliberately left untouched, same Style 1 preservation rule as page 05.
- `pages/floorsense-setup-path-00.html`'s `.complete-card` also picked up the glass-card treatment, and its single bare `.complete-card-icon` svg was wrapped in a new white/blue-bordered `.complete-card-icon-badge` (64px, matching page 01's badge but scaled up slightly to keep the glyph at its original 56px) — as a lone icon rather than part of a repeating option group, it kept plain accent blue instead of a palette colour.
- Per the site-wide propagation of `floorsense-reservation-types-01.html`'s styling onto the Smartalock branch, `pages/smartalock-it-configuration-02.html`'s `.qcard` (SSO/SCIM/API/Gallagher question cards) swapped its grey border for the same translucent-glass background/fuzzy-shadow treatment (no hover/checked state existed to convert), and its 4 raster `.qcard-icon` `<img>` icons were each wrapped in a new white/colour-bordered `.qcard-icon-badge` (92px, sized to comfortably fit the existing 76px icons) using the same blue/pink/purple/teal palette cycle as page 01, since the 4 cards form a repeating option group; the icon images themselves were left completely untouched, only the badge wrapper/border was added.
- Continuing the same propagation, `pages/smartalock-user-policy-06.html`'s single `.qcard` (Who Can Use Lockers) swapped its grey border for the same translucent-glass background/fuzzy-shadow treatment (no hover/checked state existed to convert), and its one raster `.qcard-icon` `<img>` (72px) was wrapped in a new white/`var(--accent)`-bordered `.qcard-icon-badge` (92px) — plain blue rather than a palette colour, since this page has only the one card, not a repeating option group; the icon image itself was left completely untouched.
- Same propagation applied to `pages/smartalock-locker-behaviour-03.html`'s `.dcard` (Pop Open / Push to Open) cards — its nested `.scope-panel` sub-panel was left untouched since it's a smaller `var(--radius-md)` panel, not a large card in scope. `.dcard`'s existing `:hover`/`.chosen` border-color states converted to the shadow/background-tint pattern, and its 2 raster `.dcard-icon-img` icons were each wrapped in a new `.dcard-icon-badge` (56px, sized for the existing 40px icons) using the blue/pink palette order, since Pop Open/Push to Open form a repeating 2-option group; the icon image files were left untouched.
- `pages/floorsense-default-reservation-times-07.html`'s `.info-card`, `.path-check-card` (hover/checked converted the same way as page 01), `.policy-card`, and `.policy-diagram-frame` (the latter two are dead CSS with no matching markup on this page, converted anyway for consistency) all picked up the glass-card treatment; its lone `.footer-hint-icon` — previously an unstyled flex-centering wrapper — became a small white/accent-bordered icon badge in plain accent blue (not a palette colour, since it's a single footer icon, not a repeating option group).
- `pages/pick-a-product.html`'s two `.tcard` product-choice cards (Smartalock/Floorsense) swapped their grey border for the same translucent-glass background/fuzzy-shadow treatment; the existing `:hover`/`.chosen` border-color states (driven by each card's own `--tc`/`--tc-wash` custom properties) converted to a shadow-lift plus background-tint pair so the two cards still read clearly as clickable choices.
- `pages/register.html`'s `.register-card` (defined in `css/landing.css`, used only by this page) picked up the same translucent-glass background/box-shadow treatment in place of its previous flat opaque white/no-border look; as a static form container (not a link or multi-select tile) it got no hover/checked state.
- `pages/floorsense-nsnp-11.html`'s `.path-check-card` (hover/checked converted the same way as page 01) and `.nsnp-relay-confirm` (static panel, base glass only) picked up the glass-card treatment, while `.nsnp-grace-block` was deliberately left untouched as a smaller nested sub-panel; its 4 bare `.path-icon-img` line icons were wrapped in new `.path-icon-badge` icon badges using the blue/pink/purple/teal palette order, since none of them contain the canvas-rect-plus-highlight pattern that marks the site's separate Style 1 treatment.
- `pages/smartalock-kiosk-customisation-07.html` got no CARD TREATMENT (it has no primary large `var(--radius-lg)` bordered card — `.kiosk-toggle-row`/`.kiosk-toggle-subfields`/`.kiosk-field-row` are all smaller `var(--radius-md)` sub-panels, and the tablet mockup/Lottie preview stage aren't content cards either), but its 8 dynamically-built toggle rows' raster `.ttl-icon` images were each wrapped in a new 56px `.kiosk-icon-badge` cycling the site's blue/pink/purple/teal/amber palette (a repeating option group), and the existing 46px `.preview-fallback .glyph` fallback box — a lone icon, not a repeating group — had its border recoloured from grey to `var(--accent)` blue plus the standard short drop-shadow, per the task's "likely already a badge-like box" framing. `.sidebar-icons` (inside the simulated kiosk tablet mockup) were left untouched since they render the physical device's own screen content, not the Studio's own UI.
- Continuing the same propagation, `pages/smartalock-locker-types-04.html`'s `.type-check-card` (the 7 locker-type option cards) swapped its grey border for the same translucent-glass background/fuzzy-shadow treatment, with its existing `:hover`/`.checked` border-color states converted to the shadow/background-tint pattern; the separate small `.type-tick` custom-checkbox marker was left untouched. Each of the 7 raster `.type-icon-img` icons (Top, Middle, Bottom, Large, Side, Small, Custom Type) was wrapped in a new 72px `.type-icon-badge` (a `<span>`, not a `<div>`, since it nests inside the inline `.type-label` span) cycling the full blue/pink/purple/teal/amber palette and repeating from blue for the 6th/7th icon, since these 7 cards form a repeating option group; the icon image files were left untouched.
- `pages/floorsense-fme-12.html`'s `.path-check-card` (hover/checked converted the same way as page 01) picked up the glass-card treatment; its 3 bare `.path-icon-img` line icons (All sit-stand desks, list, none) were wrapped in new `.path-icon-badge` icon badges using the blue/pink/purple palette order.
- `pages/floorsense-checkin-window-08.html`'s `.checkin-card` (single explainer card, no hover/checked state), plus its dead-CSS `.policy-card`/`.policy-diagram-frame` (no matching markup on this page, converted anyway for consistency), all picked up the glass-card treatment. Its two bare content icons — `.checkin-card-icon` (header clock) and `.checkin-footer-icon` (footer clipboard-checkmark) — were each wrapped in a white/accent-bordered icon badge (64px, scaled to keep both 56px glyphs unshrunk); as the only header/footer icons on this page's single card, both kept plain accent blue rather than a palette colour. `.policy-icon-img` (also dead CSS) was left untouched since no icon markup exists to wrap.
- `pages/floorsense-anti-desk-hogging-13.html`'s `.path-check-card` (the 2-mode enable grid, hover/checked converted the same way as page 01) and `.timeline-card.standalone` (the time-window/max-bookings settings panel, static, base glass only) picked up the glass-card treatment; its 2 bare `.path-icon-img` line icons (enable/don't enable) were wrapped in new `.path-icon-badge` icon badges using the blue/pink palette order.
- `pages/smartalock-configuration-summary-08.html`'s 8 `.summary-group` cards (IT Configuration & Integrations, Initial Locker Access Methods, Locker Door Behaviour, Reservation Type, Locker Policy, User Policy, Kiosk Customisation, Locker Types) swapped their grey border for the same translucent-glass background/fuzzy-shadow treatment (no hover/checked state existed to convert, since these are read-only summary rows), and each group heading's raster `.summary-icon` `<img>` (40px) was wrapped in a new 70px `.summary-icon-badge` cycling the blue/pink/purple/teal/amber palette across the 8 groups in DOM order (wrapping back to blue/pink/purple for the last three); `.progress-card`/`.download-card` (smaller `var(--radius-md)` panels, not the assigned card class) and `.submit-guides-panel` (a distinct accent-tinted callout) were left untouched as out of scope.
- `pages/floorsense-idle-release-09.html`'s `.checkin-card` (single explainer card with an on/off switch, no hover/checked card-level state) picked up the glass-card treatment. Its two bare content icons — `.checkin-card-icon` (header clock-with-breakaway-arrow) and `.checkin-footer-icon` (footer plain clock) — were each wrapped in a white/accent-bordered icon badge (64px, scaled to keep both 56px glyphs unshrunk); as the only header/footer icons on this page's single card, both kept plain accent blue rather than a palette colour.
- `pages/dashboard.html`'s `.configuration-card` grid (css/dashboard.css) swapped its flat opaque white background for the same translucent-glass treatment, keeping its existing `:hover { transform: translateY(-4px); }` lift but recoloring the shadow blue-tinted on hover to match the reference page; its `.section-icon` badges (previously a flat light-blue box with no border) became white-background/colored-border icon badges, with the page's only 2 icons (Smartalock, Floorsense section headers) getting `badge-blue`/`badge-pink` modifier classes added directly in the markup, since dashboard.js's inline SVGs hardcode `stroke="#0077B6"` rather than `currentColor` (a plain CSS descendant rule overrides that presentation attribute per the cascade, so no JS changes were needed).
- `pages/floorsense-reservation-length-10.html` got no CARD or ICON TREATMENT at all, by design: its only bordered elements, `.duration-option` and `.toggle-card`, are small `border-radius: var(--radius-sm)` settings rows (not the large `var(--radius-lg)` cards the propagation targets, matching the exclusion for toggle-rows/small panels), and the page has no icon markup whatsoever outside its header logo and the two excluded circular page-nav-arrow chevrons.
- Continuing the same propagation, `pages/smartalock-locker-policy-05.html`'s `.qcard` (Release when reserved but unused / Lockers per User / Release Behaviour) swapped its grey border for the same translucent-glass background/fuzzy-shadow treatment, with its existing `.visual-active` state (previously `border-color: var(--accent); box-shadow: 0 0 0 3px var(--accent-wash);`) converted to the shadow/background-tint pattern. Each of the 3 raster `.qcard-icon` `<img>` icons (72px) was wrapped in a new 88px `.qcard-icon-badge` div cycling blue/pink/purple, since the 3 cards form a repeating option group. Per an earlier attempt on this same page that was reverted for recoloring the icons themselves, this change touches only the badge div's own border color — the icon `<img>` elements and their underlying PNG files were left 100% byte-for-byte untouched (confirmed via `git diff --stat` showing no changes to the 3 asset files).
- `pages/project-home.html`'s 3 `.feature-card` tiles (FLOORSENSE, SMARTALOCK, AI Policy Workshop — each a clickable div with its own `onclick` navigation, styled via this page's own inline `<style>` block rather than an external stylesheet) swapped their solid light-blue background/`2px solid #0077B6` border for the same translucent-glass treatment, converting the existing `:hover { background; border-color; }` state to a shadow-lift + background-tint pair while keeping the existing `translateY(-4px)` lift; the page has no icon markup at all, so no icon-badge work applied. An unrelated pre-existing bug was found and flagged (not fixed, out of scope): the `.hero-content` div is never closed before `</header>`, unbalancing the tag tree.
- `pages/smartalock-access-methods-09.html`'s `.access-check-card` (Building access cards / PIN / Floorsense app / QR code) swapped its grey border for the same translucent-glass background/fuzzy-shadow treatment, with its existing `:hover`/`.checked` border-color states converted to the shadow/background-tint pattern. Each of the 4 raster `.access-icon-img` icons (56px) was wrapped in a new 72px `.access-icon-badge` cycling the blue/pink/purple/teal palette, since the 4 cards form a repeating option group; the separate `.access-tick` custom-checkbox marker was left untouched, and the icon image files themselves were left byte-for-byte unchanged.
- `pages/reports.html`'s 6 `.feature-card` report sections (Project Summary, Implementation Progress, Locker Policy Summary, Access Configuration, Technical Configuration, Validation Status) were confirmed to be the same repeating static-panel card type before applying the glass treatment uniformly to `.feature-card`'s shared base rule in `css/landing.css`; that stylesheet's separate global "CLICKABLE FEATURE CARDS" hover rule (pre-existing, applies `cursor:pointer` + a hover lift to every `.feature-card` site-wide, including these non-interactive report cards) had its hover shadow recolored to the same blue-tinted variant used elsewhere, for consistency. No icon markup exists on any of the 6 sections. A pre-existing, unrelated stray extra `</div>` in the first card's markup was flagged, not fixed.
- `pages/floorsense-out-of-box-policy.html`'s `.summary-group`, `.core-policy-card`, and `.policy-card` all picked up the base glass-card treatment (none had a pre-existing hover/selected border-color state); `.summary-group`'s own box-model is currently fully overridden by the higher-specificity `.core-policy-body .summary-group` rule (every actual instance lives inside `.core-policy-card`'s single canvas), so this update has no visible effect today but was applied anyway for site-wide consistency. Its lone `.download-card-icon` and 6 `.summary-icon`s were wrapped in new white/colored-border icon badges — the download icon in plain accent blue (a single lone icon), the 6 summary icons cycling the blue/pink/purple/teal/amber palette (wrapping back to blue for the 6th). `.submit-guides-icon` was deliberately left untouched despite being one of the three assigned icon classes: it's an existing solid-accent-circle checkmark confirmation glyph — UI chrome, not a content icon, matching this page's own prior documented convention for status/warning glyphs.
- `pages/smartalock-templates.html`'s 3 `.template-card` deployment-template cards (Fixed Lockers, Long-Term Adhoc, Daily Adhoc — styled via this page's own dedicated `css/templates.css`) swapped their `border: 2px solid transparent` for the same translucent-glass background/box-shadow treatment, with the existing `:hover`/`.selected` border-color states converted to the shadow/background-tint pattern while keeping the existing `translateY(-6px)` hover lift; the page has no icon markup at all, so no icon-badge work applied.
- On a follow-up review of `pages/smartalock-templates.html`, its `.summary-card` ("Selected Templates" panel) turned out to have been missed — it was still a flat opaque white card with a plain shadow and no border to begin with. Gave it the same translucent-glass background/backdrop-filter and two-layer fuzzy shadow as `.template-card` above it, for visual consistency; it has no hover/selected state, so none was added.
- `smartalock-templates-00.html` (the root-level "Project Registration" page — the site's true first-opened entry point, missed by the earlier sitewide propagation since it lives outside `pages/`) had both its `.qcard` and icons brought in line: the card swapped its grey border for the same translucent-glass background/fuzzy-shadow treatment, and each of its 3 raster `.qcard-icon` icons (Company Details, Contacts, Project Dates) was wrapped in a new 92px `.qcard-icon-badge`, matching the identical pattern already used on `smartalock-it-configuration-02.html`'s own qcards; the icon image files themselves were left untouched.
- On the same page, per a follow-up request that each icon badge's border match the icon symbol inside it, the initial arbitrary blue/pink/purple assignment was replaced with colors sampled directly from each PNG's own glyph pixels (median RGB via PIL): `#0071E3` for Company Details (the site's own accent blue), `#0E8F8F` (dark teal) for Contacts, and `#D98E00` (burnt amber) for Project Dates.
- `pages/smartalock-reservation-type-01.html`'s `.tcard` (Allocated by Administrator / User Self-Allocated / Parcel Delivery) swapped its grey border for the same translucent-glass background/fuzzy-shadow treatment; since this card pre-dates the propagation with its own per-type accent (`--tc`/`--tc-wash`, blue/green/purple per `data-type`), a matching `--tc-glow`/`--tc-glow-strong` pair was added per type so the hover/`.chosen` shadow still tints in that card's own colour rather than flattening to one shared blue. Each of the 3 raster `.tcard-icon-img` icons (56px) was wrapped in a new 72px `.tcard-icon-badge` cycling the site's standard blue/pink/purple palette (independent of each card's own `--tc` theme colour, matching the convention used elsewhere on the site); `.duration-panel` (a smaller `var(--radius-md)` sub-panel) was left untouched.
- `pages/smartalock-wizard.html`'s single `.overall-progress-card` (its own inline `<style>` block, not an external stylesheet) swapped its flat opaque white background for the same translucent-glass background/box-shadow treatment; as a static progress-display panel with no click/selected state, no hover was added, and no icon markup exists on this page.
- `pages/floorsense-desk-booking-guide-live.html` (the live end-user PDF-guide preview) got icon treatment only — reviewed for large bordered card elements and found none, since `.not-on-box`'s dashed border intentionally signals "not switched on yet" (an off/placeholder look, deliberately left untouched) and `.footer-note` is a small flat disclaimer strip, not a content card. `.hero-icon-badge` (the 4 hero icons) picked up a border plus the standard short drop-shadow and 4 new `badge-blue`/`badge-pink`/`badge-purple`/`badge-teal` modifier classes recoloring just the badge border (glyphs stay white, since a colored glyph would vanish against this page's own blue gradient hero). `.row-icon` (13 content-row icons, one per distinct topic rather than a multi-select option group) deliberately kept its existing plain-blue treatment rather than palette-cycling, to avoid competing with the page's existing `.row-icon.warm` amber "pay extra attention" convention already used on 2 of those rows — it only gained the same short drop-shadow for consistency.
- `pages/smartalock-locker-guide-live.html` (the Smartalock twin of the Floorsense live end-user guide above) got the identical icon-only treatment for the identical reasons: no large bordered card elements to convert (`.not-on-box`'s dashed border and `.footer-note`'s small flat strip both left untouched), `.hero-icon-badge`'s 4 icons gained a border plus short drop-shadow and `badge-blue`/`badge-pink`/`badge-purple`/`badge-teal` modifier classes (glyphs staying white against the same blue gradient hero), and `.row-icon` (7 instances, one per distinct topic) kept its existing plain-blue treatment plus the same short drop-shadow, to avoid diluting its own existing `.row-icon.warm` amber convention (used on the idle-release row).
- `pages/floorsense-anti-desk-hogging-13.html` had its decorative `.eyebrow` pill labels removed ("Additional Features · Optional" above the hero title and "Configure" above the section heading), plus all 3 related CSS rulesets from its inline `<style>` block, per a site-wide request to strip the eyebrow element from every Floorsense page; both parent containers kept their other heading/paragraph content, so no container was deleted.
- `pages/floorsense-nsnp-11.html` had its 2 `.eyebrow` pill labels removed ("Additional Features · Optional" above the hero title and "Configure" above the section heading), plus the `.eyebrow`/`.panel-intro .eyebrow`/`.section-head .eyebrow` CSS rulesets from its inline `<style>` block, per the same site-wide eyebrow-removal request; both parent containers kept their other content, so no container was deleted.
- `pages/floorsense-business-hours-05.html` had its single "Getting Started" `.eyebrow` pill above the hero title removed, along with the base `.eyebrow` rule and `.panel-intro .eyebrow` modifier from its inline `<style>` block, per the same site-wide eyebrow-removal request; the `.panel-intro` container kept its `<h1>`/`<p>` content, so nothing else was deleted.
- `pages/floorsense-optional-features-06.html` had its single "Additional Features · Optional" `.eyebrow` pill above the hero title removed, along with the base `.eyebrow` rule and `.panel-intro .eyebrow` modifier from its inline `<style>` block, per the same site-wide eyebrow-removal request; the `.panel-intro` container kept its `<h1>`/`<p>` content, so nothing else was deleted.
- `pages/floorsense-checkin-methods-02.html` had both its `.eyebrow` pills removed ("Getting Started" above the hero title, "Configure" above the section heading) plus all 3 related CSS rulesets from its inline `<style>` block, per the same site-wide eyebrow-removal request; both parent containers kept their other heading/paragraph content, so no container was deleted.
- `pages/floorsense-checkin-window-08.html` had its single "Getting Started" `.eyebrow` pill above the hero title removed, along with all 3 related CSS rulesets (base `.eyebrow`, `.panel-intro .eyebrow`, and a dead `.section-head .eyebrow` with no matching markup on this page) from its inline `<style>` block, per the same site-wide eyebrow-removal request; the `.panel-intro` container kept its `<h1>`/`<p>` content.
- `pages/floorsense-out-of-box-policy.html` had all 3 `.eyebrow` pills removed ("Getting Started" above the hero title, "Review" above the summary columns, and "Neighbourhood Policies" above the "Additional Policies" heading), plus the base `.eyebrow`, `.panel-intro .eyebrow`, and `.section-head .eyebrow` CSS rulesets from its inline `<style>` block, per the same site-wide eyebrow-removal request; the "Review" span's parent `.section-head` had no other content, so that now-empty div was deleted entirely, while the other two parents kept their remaining heading/paragraph content.
- `pages/floorsense-default-reservation-times-07.html` had its single "Getting Started" `.eyebrow` pill above the hero title removed, along with all 3 related CSS rulesets (base `.eyebrow`, `.panel-intro .eyebrow`, and a dead `.section-head .eyebrow` with no matching markup on this page) from its inline `<style>` block, per the same site-wide eyebrow-removal request; the `.panel-intro` container kept its `<h1>`/`<p>` content.
- `pages/floorsense-desk-user-groups-03.html` had all three "Configure" `.eyebrow` pills removed (above the hero title, and above each of the Desk Group Setup/User Group Setup section headings) plus all 3 related CSS rulesets from its inline `<style>` block, per the same site-wide eyebrow-removal request; all three parent containers kept their other heading/paragraph content, so no container was deleted.
- `pages/floorsense-prebooking-advance-04.html` had both its `.eyebrow` pills removed ("Getting Started" above the hero title, "Configure" in the section head), plus the base `.eyebrow` and `.section-head .eyebrow` CSS rulesets from its inline `<style>` block, per the same site-wide eyebrow-removal request; the "Configure" span's parent `.section-head` already had no other content (an earlier edit had removed its `<h2>`/paragraph), so that now-empty div was deleted entirely rather than left as a bare shell.
- `pages/floorsense-fme-12.html` had both its `.eyebrow` pills removed ("Additional Features · Optional" above the hero title, "Configure" above the section heading) plus all 3 related CSS rulesets from its inline `<style>` block, per the same site-wide eyebrow-removal request; both parent containers kept their other heading/paragraph content, so no container was deleted.
- `pages/floorsense-reservation-length-10.html` had all 3 `.eyebrow` pills removed ("Getting Started" above the hero title, and "Configure" above each of the Default Reservation Length/Maximum Desks Per User section headings) plus all 3 related CSS rulesets from its inline `<style>` block, per the same site-wide eyebrow-removal request; all three parent containers kept their other heading/paragraph content, so no container was deleted.
- `pages/floorsense-idle-release-09.html` had its single "Additional Features · Optional" `.eyebrow` pill above the hero title removed, along with all 3 related CSS rulesets (base `.eyebrow`, `.panel-intro .eyebrow`, and a dead `.section-head .eyebrow` with no matching markup on this page) from its inline `<style>` block, per the same site-wide eyebrow-removal request; the `.panel-intro` container kept its `<h1>`/`<p>` content.
- `pages/floorsense-reservation-types-01.html` had both its `.eyebrow` pills removed ("Getting Started" above the hero title, "Configure" in the section head), plus the base `.eyebrow`, `.panel-intro .eyebrow`, and `.section-head .eyebrow` CSS rulesets from its inline `<style>` block, per the same site-wide eyebrow-removal request; the "Configure" span's parent `.section-head` had no other content, so that now-empty div was deleted entirely, while the hero's `.panel-intro` kept its `<h1>`/`<p>` content.
- `pages/floorsense-setup-path-00.html` had its single "All Set" `.eyebrow` pill above the hero title removed, along with the base `.eyebrow` rule and `.panel-intro .eyebrow` modifier from its inline `<style>` block, per the same site-wide eyebrow-removal request; the `.panel-intro` container kept its `<h1>`/`<p>` content, so nothing else was deleted.
- `pages/smartalock-it-configuration-02.html`'s 4 `.qcard-icon-badge` modifiers (SSO/SCIM/API/Gallagher) had their border colors replaced with values sampled directly from each icon PNG's own glyph pixels (median RGB via PIL) instead of the previous arbitrary blue/pink/purple/teal rotation: `#0655D1` (SSO), `#118B2F` (SCIM), `#1050C7` (API), `#4C1FB8` (Gallagher). Renamed the modifier classes from generic color names to per-icon names (`.icon-sso`, `.icon-scim`, `.icon-api`, `.icon-gallagher`) since SSO and API both land on a shade of blue.
- `pages/smartalock-locker-behaviour-03.html`'s `.dcard-icon-badge` Pop Open/Push to Open badges swapped their arbitrary blue/pink rotation for colors sampled directly from each icon PNG's own pixels: Pop Open's blue tightened from `#4A8DF0` to `#0071E3` (still `badge-blue`), and Push to Open changed from `#EC6BA5` pink to `#D98E00` amber (class renamed `badge-pink` to `badge-amber`).
- `pages/smartalock-locker-types-04.html`'s 7 `.type-icon-badge` icons swapped their blue/pink/purple/teal/amber rotation for colors sampled directly from each icon PNG's own pixels — Top, Middle, Bottom, Large, Side, and Small turned out to be the exact same blue (`#0071E3`, all now sharing one `badge-blue` class) and only Custom Type is a genuinely different color (`#8B5CF6` purple, `badge-purple`); the now-unused `badge-pink`/`badge-teal`/`badge-amber` CSS rules were removed.
- `pages/smartalock-locker-policy-05.html`'s 3 `.qcard-icon-badge` icons had their border colors replaced with values sampled directly from each icon PNG's own pixels instead of the previous arbitrary blue/pink/purple rotation: Release when reserved but unused tightened from `#4A8DF0` to `#2552CF` blue (still `badge-blue`), Lockers per User changed from `#EC6BA5` pink to `#0D8217` green (class renamed `badge-pink` to `badge-green`), and Release Behaviour tightened from `#9B6BEC` to `#472ABC` purple (still `badge-purple`).
- `pages/smartalock-user-policy-06.html`'s lone `.qcard-icon-badge` (Who Can Use Lockers) was overridden from its plain `var(--accent)` blue "lone icon" convention to a new `badge-teal` modifier class, border-color `#0E8F8F`, sampled directly from `icon-who-can-use-lockers.png`'s own pixels — the actual icon is teal, not blue.
- `pages/smartalock-access-methods-09.html`'s 4 `.access-icon-badge` icons swapped their arbitrary blue/pink/purple/teal rotation for colors sampled directly from each icon PNG's own pixels: Building Access Cards changed from blue to `#328456` green, PIN changed from pink to `#8E7645` amber (a blended approximation since that icon is multi-colored), Floorsense App changed from purple to `#0A5E5F` teal, and QR Code changed from teal to `#214A74` blue.
- `pages/smartalock-reservation-type-01.html`'s 3 `.tcard-icon-badge` icons had their border colors replaced with values sampled directly from each icon PNG's own pixels: Allocated by Administrator tightened from `#4A8DF0` to `#0071E3` blue (still `badge-blue`), User Self-Allocated changed from `#EC6BA5` pink to `#1FAA59` green (class renamed `badge-pink` to `badge-green`), and Parcel Delivery tightened from `#9B6BEC` to `#8B5CF6` purple (still `badge-purple`); this is unrelated to and does not touch the page's own separate per-card `--tc`/`--tc-wash` theming.
- `pages/smartalock-configuration-summary-08.html`'s 8 `.summary-icon-badge` icons swapped their cycled blue/pink/purple/teal/amber rotation for colors sampled directly from each icon PNG's own pixels; 4 of the 8 (IT Configuration, Locker Door Behaviour, Reservation Type, Kiosk Customisation) turned out to share the exact same blue `#0071E3`, Locker Policy is a distinctly different, richer blue `#2552CF`, Initial Locker Access Methods is `#1FAA59` green, User Policy is `#0E8F8F` teal, and Locker Types is a neutral `#48484A` grey (genuinely monochrome line-art) — since shared color-family names could no longer disambiguate the two blues, the old `badge-*` classes were replaced with 8 distinct per-icon classes (`.icon-it-config`, `.icon-access-card`, `.icon-locker-door`, `.icon-reservation-type`, `.icon-release-idle`, `.icon-who-can-use`, `.icon-kiosk-custom`, `.icon-locker-types`).
- `pages/smartalock-kiosk-customisation-07.html`'s 8 dynamically-built `.kiosk-icon-badge` toggle icons were sampled the same way and turned out to be a consistent monochrome-blue icon set — all 8 PNGs are the exact same `#0071E3` accent blue, which the base badge rule's `border: 1.5px solid var(--accent)` already provided. The `BADGE_PALETTE` array and per-row `badgeClass` cycling were removed from `buildToggleList()` in the `<script>` block (toggle/switch/save logic untouched), and the now-unused `.badge-blue`/`.badge-pink`/`.badge-purple`/`.badge-teal`/`.badge-amber` CSS rules were deleted.
- `pages/floorsense-prebooking-advance-04.html`'s photographic `.policy-thumb` illustration (assets/images/advance-booking-illustration.png) was removed from the card and replaced with a small `.policy-icon-badge` glyph (a hand-drawn calendar-with-forward-arrow icon) matching the site's standard flat white/blue-border badge language. A new `.policy-illustration` banner — a hand-authored, pure-outline inline SVG timeline diagram (calendar at "Today," a rail out to "Day 30," a bracket labeled "Book up to 8 days ahead," a flag at "Day 30 maximum") — was added directly above the card, sized to the same 760px max-width so it lines up edge-to-edge with the card below; no shape fills, gradients, or background fill were used, only the accent blue stroke color (text labels excepted, which need `fill` to render).
- Fixed a Reset button bug on `pages/floorsense-desk-user-groups-03.html`: `resetBtn`'s handler was reassigning `state.deskGroups`/`state.userGroups` to brand-new default objects, but `wireGroup()`'s closures (set up once at page load) still held references to the *original* objects, so Reset silently saved the correct defaults to localStorage while leaving every checkbox showing its pre-reset state on screen, and any checkbox ticked afterward stopped saving entirely until the next page load. Confirmed via a jsdom repro, then fixed by mutating the existing objects in place (`Object.assign(state.deskGroups, DEFAULT_DESK_GROUPS)`) instead of reassigning them, and re-verified the same repro now passes (Reset correctly restores both grids' checkboxes, hides/clears the policy subfields, and subsequent clicks save in sync with the DOM).
- On `pages/floorsense-prebooking-advance-04.html`, swapped the hand-drawn `.policy-icon-badge` calendar SVG for a user-supplied icon image instead — a rounded-square app-icon-style graphic (clock + calendar + "A" glyph with up-arrows in indigo line art) saved to `assets/images/icon-advance-booking.png`. Since the artwork already bakes in its own white canvas and rounded border, it's placed directly as a plain `.policy-icon-img` with no extra wrapping badge div, sized to 72px with the same short drop-shadow used on every other icon badge site-wide.
- On the same page, per a follow-up request swapped in a second user-supplied icon (a clock + calendar-grid variant of the same indigo app-icon style, saved as `assets/images/icon-advance-booking-v2.png` since the original file couldn't be overwritten) and removed the `.policy-illustration` banner entirely — both its hand-authored SVG timeline diagram and its CSS — so the page now shows only the single card with its icon, no banner above it.
- On the same page again, per a request for "your own separate version" of that icon rather than the uploaded reference image, hand-drew an SVG re-creation of the same clock-overlapping-calendar composition and reverted from the plain `<img class="policy-icon-img">` back to the `.policy-icon-badge`/`.policy-icon-glyph` white-canvas/blue-border wrapper pattern used elsewhere on the site — open blue strokes only, no fills, no reference to either PNG asset (both left in place, unreferenced).
- On the same page, redrew the hand-drawn `.policy-icon-glyph` again to more closely match a further user-supplied reference (more clock tick marks — 8 compass points instead of 4 — and a denser 3x3 calendar grid), and fixed a rendering artifact along the way: the clock's hour/minute hands were split into two separate `<line>` elements instead of one connected `<path>`, since the single path had been filling in as a solid wedge shape at the obtuse angle between the hands rather than reading as two thin lines.
- On `pages/floorsense-prebooking-advance-04.html`, added a days-ahead segmented timeline slider below the existing icon/description/number-field row, copied class-for-class from `pages/floorsense-business-hours-05.html`'s Arrival/Departure diagram (glass segmented zone with inset glow/gloss sheen, rail, per-day tick marks, draggable pin with a floating pill label, scroll-in reveal animation) so the two controls look and feel identical. Since this page has one value instead of two independent endpoints, the rail's 0-day mark is a fixed anchor and the single pin drives the existing `#prebookingAdvanceDays` field/status-hint/save plumbing directly, so the slider, the numeric field, and localStorage can never drift out of sync. Verified via a jsdom test driving synthetic drag/keyboard/typing/Reset events across the full 0-30 day range.
- On `pages/floorsense-prebooking-advance-04.html`, per a follow-up request to use the reference image directly rather than a hand-drawn recreation, replaced the `.policy-icon-badge`/`.policy-icon-glyph` SVG with the user-supplied "advanced booking icon.png" itself (a clock+calendar glyph on a lavender circular backdrop), saved to `assets/images/icon-advance-booking-v3.png` and placed directly as a plain `.policy-icon-img`, matching the same approach used for this page's two earlier raster-icon swaps.
- On `pages/floorsense-reservation-types-01.html`, made "Reserved by User as Needed" (`data-template="adhoc"`) the default reservation type: a new `DEFAULT_RESERVATION_TYPES = ['adhoc']` constant is used both as the initial `state.selected` fallback (whenever `reservationTypes` has never been saved) and by the Reset button (which now restores just `adhoc` instead of clearing every card). A user who leaves the page untouched now sees "Reserved by User as Needed" pre-selected with Continue already enabled, and pressing Save & continue persists that choice.
- On `pages/floorsense-out-of-box-policy.html`, added a new "Desk Allocation Method" summary group (first on the page) showing whichever reservation type(s) are currently selected on `pages/floorsense-reservation-types-01.html` — defaulting to "Reserved by User as Needed" to match that page's new default — with a group-level Edit link back to it. Multiple selected types join with " · ", and the row's description uses the shared `joinOxford()` helper for natural "a, b, and c" phrasing.
- On `pages/floorsense-desk-user-groups-03.html`, made "We will not use User Groups feature initially (will rely on Desk Group and General policy)" the default User Group option (matching Desk Groups' existing "We will not use Desk Groups" default): `DEFAULT_USER_GROUPS` changed from `{ none: false, provide: true, policy: true, policyNotes: "..." }` to `{ none: true, provide: false, policy: false, policyNotes: "" }`, so both a fresh page load and pressing Reset now show "none" ticked and the other two options unticked for both Desk Groups and User Groups alike.
- Fixed a layout overflow on `pages/floorsense-out-of-box-policy.html`'s new "Desk Allocation Method" row: `#reservationTypesSummaryValue` could produce a long " · "-joined string (when several reservation types are selected on page 01) that overflowed its `white-space: nowrap` value span and overlapped the row's label. Added a `.summary-row-top.wrap` modifier (stacks the value below the label and lets it wrap normally) applied only to this row, leaving every other row's layout untouched.
- On `pages/floorsense-prebooking-advance-04.html`, rolled back the raster-image icon swap: reverted from `<img class="policy-icon-img" src="../assets/images/icon-advance-booking-v3.png">` back to the hand-drawn `.policy-icon-badge`/`.policy-icon-glyph` SVG (the 8-tick/dense-grid clock-overlapping-calendar redesign). `icon-advance-booking-v3.png` itself is unchanged on disk and was left in place, now unreferenced again.
- On the same page, swapped the icon again to `assets/images/icon-dates.png` (the calendar-on-pale-amber-circle icon already used for "Project Dates" on `smartalock-templates-00.html`), placed directly as a plain `.policy-icon-img` since the artwork bakes in its own circular backdrop. `.policy-icon-badge`/`.policy-icon-glyph` were removed again.
- On `pages/floorsense-desk-user-groups-03.html`, removed the `.groups-illustration` banner (the HR/Client Care neighbourhood image sitting between the top caution notice and the Desk/User Group option columns) along with its now-unused CSS. The image asset itself was left in place, unreferenced.
- On the same page, removed the amber `.setup-notice.page-top-notice` caution banner entirely, made the two `.groups-card` columns equal height (`.groups-columns` switched from `align-items: start` to `stretch`), and bottom-aligned each card's option list (`.groups-card` became a flex column with `margin-top: auto` on `.group-check-grid`) so the Desk Group and User Group option lists line up across both columns regardless of intro-text length.
- Fixed stray per-section shadows on `pages/floorsense-out-of-box-policy.html`: each of the 6 `.summary-group` sections inside `.core-policy-card` was independently casting the full glass-card box-shadow (the `.core-policy-body .summary-group` override zeroed background/border/radius/padding/max-width but not box-shadow), making every section from "Desk Allocation Method" onward look crammed against its own edges. Added `box-shadow: none` to that override so only the single outer `.core-policy-card` casts a shadow; per-icon shadows are untouched.
- On `pages/floorsense-checkin-methods-02.html`, removed the leftover `.desk-tick` checkbox squares (22x22px, 3px border) next to each option's icon badge — this was the last page still shipping that element after `floorsense-reservation-types-01.html` dropped it in an earlier pass, since a card's `.checked` state already reads clearly through its own background tint and shadow. Icon badges now sit flush at each card's left edge, matching page 01.
- Excluded `pages/floorsense-setup-path-00.html` from the wizard's linear flow: `pages/floorsense-optional-features-06.html` is now the last of the core selections, with its Continue button relabelled "Continue to Summary" and its next-arrow/redirect pointing straight to `pages/floorsense-out-of-box-policy.html`; that page's back-arrow now points back to Optional Features to match. The progress bar's "Core Complete" segment (still linking to `floorsense-setup-path-00.html`) was left untouched on every page, so that page stays directly reachable — only the enforced Continue/back-arrow path changed.
- On `pages/floorsense-optional-features-06.html`, applied the same distinct-icon-colour rule already used on pages 01 and 02: each of the 4 `.feature-row-icon`s (previously all plain accent blue) now cycles blue/pink/purple/teal in DOM order via a `badge-COLOR` modifier class, matching the exact hex values used by the icon badges elsewhere.
- Removed `floorsense-setup-path-00.html`'s "Core Complete" segment from the progress bar entirely, on all 15 Floorsense wizard pages — it's no longer a directly-clickable shortcut anywhere. The bar now shows 6 segments ending at "Optional Features". `floorsense-setup-path-00.html` itself is unchanged and still reachable directly by URL; its own back-arrow and Continue button still work as before.
- Wrapped `floorsense-prebooking-advance-04.html`'s bare `icon-dates.png` icon in a new `.policy-icon-badge` white square canvas with a `1.5px solid #D98E00` amber border — the same sampled amber value already used to border this exact icon in `smartalock-templates-00.html`'s `.qcard-icon-badge.badge-amber` — matching that page's icon-canvas convention instead of leaving the image floating with no border line.
- Refined that same badge's size and line weight to sit closer to `floorsense-business-hours-05.html`'s bare 84px "Style 1" icon: shrank the badge from 88px to 84px, eased its radius from 24px to 18px, thickened its border from 1.5px to 2px, and shrank the inner image from 72px to 58px so it leaves a margin closer to that page's own icon proportions — same colour/content, just tighter and crisper.
- Removed the NSNP row/switch from `floorsense-optional-features-06.html`'s hub — NSNP itself is untouched and still fully configurable from `floorsense-nsnp-11.html`'s own mode grid and from the Configuration Summary's row-edit-link, only this hub's on/off shortcut is gone. The hub now lists 3 features instead of 4; fixed a latent array-index bug this exposed in the FME acknowledgment checkbox by looking it up by key instead of by position.
- Added two new options to `floorsense-optional-features-06.html`'s hub, now 5 rows total: "Remote Booking Confirmation" (off by default, plain switch — lets end users confirm a booking remotely with a tap instead of checking in at the desk; new `remoteConfirm.enabled` state) and "Recurring Bookings" (off by default, with an inline Mon-Sun day-of-week picker shown once enabled — reuses the same `outOfBoxPolicy.recurringEnabled` field the Configuration Summary's own toggle already uses, plus a new `outOfBoxPolicy.recurringDays` array seeded Mon-Fri by default). Both new icons follow the page's existing "Style 1" canvas convention.
- Re-centered the Remote Booking Confirmation icon, then simplified it further by removing the tap-signal motif entirely, leaving just a centered ticked circle. Redrew the Recurring Bookings icon as a literal 2x2 grid — a square split into four cells, two of them (diagonally opposite) shaded and ticked — replacing the earlier repeat-arrow glyph with a more literal "pick which days apply" symbol.
- Redrew the Anti Desk Hogging icon as a solid-silhouette bird sitting in a nest (keeping its existing highlight circle unchanged), replacing the earlier repeat-arrow-with-a-slash glyph; nudged the bird down afterward so it visibly sits inside the nest bowl rather than perched above it.
- Redrew the FME (Follow Me Ergonomics) icon on `floorsense-optional-features-06.html` as two separate vertical arrows, one up and one down, replacing the earlier single up-arrow-on-a-pole glyph; then simplified it further to just the two arrow tips inside the page's standard highlight circle, dropping the shafts and base line.
- Removed Recurring Bookings' inline day-of-week picker from `floorsense-optional-features-06.html` — it's back to a plain on/off switch (still reusing `outOfBoxPolicy.recurringEnabled`), with the "chosen days of the week" wording dropped from its description.
- Synced `floorsense-out-of-box-policy.html`'s Configuration Summary with today's `floorsense-optional-features-06.html` changes: added a live "Remote Booking Confirmation" toggle to the Additional Options group (new `remoteConfirm.enabled` state, same switch pattern as Recurring bookings/Privacy, kept in sync with the hub's own switch). NSNP's row and Recurring bookings' existing toggle needed no changes — NSNP itself and `outOfBoxPolicy.recurringEnabled` were both unaffected by today's hub-only edits.
- Synced the "Explain this policy to your end users" live guide (`floorsense-desk-booking-guide-live.html`) with the same page 06 changes: added a new conditional row for Remote Booking Confirmation (`remoteConfirm.enabled`), shown when on and listed under "Not switched on yet" when off, matching the NSNP/Sit-stand/Recurring pattern. NSNP's and Recurring's existing rows needed no changes.
- Restyled all three timeline diagrams on `floorsense-default-reservation-times-07.html` to match `floorsense-business-hours-05.html`'s finalized look: a taller pill-shaped rail, a segmented deep-to-light-blue gradient zone (cards 2/3 only) built from individually rounded 30-minute blocks with a glassy sheen, fully invisible drag-handle pins revealed only on hover/drag/focus, floating pill-chip time labels, and small-caps dot/dash tick marks below the rail instead of crossing through it — replacing the old thin rail, visible pins, flat-gradient zone, and the oversized "center-node"/"big-anchor" pin treatment (now removed) used for each card's start/end reference points.
- Fixed `floorsense-business-hours-05.html`'s grey timeline panel touching the outer card's left/right edges with no gutter — `.timeline-card` now has a 22px horizontal margin (matching the header/footer's inset) and its own 14px border-radius, so it reads as an inset floating panel instead of a full-bleed rectangle.
- Moved `floorsense-business-hours-05.html`'s earliest selectable Arrival time from 6:00 AM to 4:30 AM by changing the shared `RAIL_START_MIN` constant, which also shifted the rail's first tick and the Arrival pin's drag/keyboard clamp floor to match. Departure's latest time (10:00 PM) is unchanged.
- Restored the "Core Complete" progress-bar segment (linking back to `floorsense-setup-path-00.html`) to `floorsense-out-of-box-policy.html`'s own progress bar, ahead of its "Configuration Summary" segment — this segment had been removed sitewide in an earlier change; it's now brought back on the summary page specifically so there's a way back to the wizard from there, while the other 14 Floorsense wizard pages keep the segment removed.
- Repointed that restored first segment away from `floorsense-setup-path-00.html` (now excluded from the wizard's linear flow) to `floorsense-reservation-types-01.html` instead — the actual first wizard step — matching that module's own data-module/href/tip exactly so it also tracks that module's real saved/current state.
- Made `floorsense-default-reservation-times-07.html`'s "Start time if booked in advance" and "Reservation ends after a fixed number of hours" card descriptions show the actual configured default start/end times inline, replacing a static "book at 8:30 AM or 1:00 PM" example with a live sentence computed from the real start time and duration. Confirmed `floorsense-checkin-window-08.html`'s own start-time example already reads the same live configured value — no change needed there.
- Added the default reservation start time alongside the duration on `floorsense-out-of-box-policy.html`'s "Default reservation duration" summary row (now shows e.g. "8:30 AM – 5:30 PM (9 hours)" and a matching description), and added the same dynamic start-time-and-duration/end sentence to `floorsense-desk-booking-guide-live.html`'s "Plan ahead" section, so end users see the actual configured default too — both read live from `project.floorsense.wizard.defaultReservationTimes`.
- Removed the circular radio-dot indicator (`.path-tick`) from `floorsense-nsnp-11.html`'s 4-option mode grid — the selected card is now distinguished by its own existing blue highlight/shadow alone, with no separate selector widget. The underlying radio inputs and click-to-select behavior are unchanged; the identical `.path-tick` pattern on other pages (default-reservation-times-07, fme-12, anti-desk-hogging-13) was left as-is.
- On `floorsense-idle-release-09.html`, replaced the header icon with the footer's plain clock-face glyph and removed the footer icon entirely (footer now shows text only, matching `floorsense-business-hours-05.html`'s icon-less footer pattern), and renamed the diagram's fixed left-hand pin label from "Reserved" to "Reservation Start".
- Follow-ups on `floorsense-idle-release-09.html`'s timeline: shortened "Reservation Start" to "Start" and moved that fixed pin to the rail's true left edge (was inset 10% like the draggable end pin); removed the redundant "0h" chip that floated above it; gave the grey timeline panel the same 22px inset + rounded corners already applied to `floorsense-business-hours-05.html`'s panel; and shortened the draggable duration's maximum range from 24 hours to 12 hours.

## Status

Wizard flow (10 modules above): feature-complete prototype, actively maintained.
Rest of the app: earlier-stage scaffolding.

## Technologies

- HTML5, CSS3 (Inter via Google Fonts), vanilla JavaScript
- `localStorage` for state — no backend
- Lottie (`lottie-web` via CDN) for the Locker Door Behaviour animations

## Author

Oleg Ovchinnikov
